# 评审报告 · 诚实性与对抗性

> 评审对象：`plan/memory-web/` 的 6 份设计文档 + `00-共同上下文.md`（冻结契约）。
> 评审人定位：**找假话**，不是找优点。评审期 2026-09-15，主 agent 在本批评审期内推进了契约变更 #6–#11。
> 纪律：只读被评审文档，未修改任何一行。所有结论带 `文件:行`。可复现 spike 在 `/tmp/spike*.mjs`（用后即删）。
>
> ⚠️ **本文记录「我读文档那一刻」的状态。** 评审期内主 agent 已根据我上报的 P17 回写契约 §8.1，并新增 §16 跨模块接口裁定。**§16 落地后，本文 B3/B4/B6 中的若干条已被裁定——但裁定不等于文档已改，仍以各文档最新版为准。**

---

## ① 结论

**不可进入实现。** 7 条 blocker：2 条是「声称已实跑、实测为假」的伪造证据；3 条是跨文档字段级矛盾（功能按文档写不出或必然报错）；1 条是 D6 把契约已明文禁止的提取法又推荐回来（会让 parity 测试**假绿**）；1 条是**「非空性断言」——这批文档回答得最差的一题**（见 B7）。

同时确认：契约 §7.4 / §6.6 / §8.3 三处最高风险的冻结论断，我逐条实测后**全部成立**（见 §六）。**这批文档不是全错**，错的集中在两类：**「声称实跑却没跑」**与**「旧口径没同步」**。

---

## ② Blocker

### B1 ⭐ D1 声称「契约缺 `/api/raw` 与 `/api/sessions` 两行」——假现状断言

- **位置**：`01-服务端与API.md:283`、`:1050`（§11-C1）；`:119` 引用它。
- **原文**（`:283`）：「⚠️ §7.1 契约表未列 `/api/raw` 与 `/api/sessions`…**这是我在本范围内发现的契约缺口**」；（`:1064-1066`）「契约 §7.1 表里只有 `/api/meta`…`/api/events` 与 11 个 POST，**没有** `/api/raw`…**契约内部前后不一致**」。
- **实测**：契约 `:301` = `| GET | /api/raw | …`，`:302` = `| GET | /api/sessions | …`。两行都在，`/api/raw` 行连 `total`/`totalAll`/`before` 都写全。GET 行实为 **12 行**。主 agent 亦独立确认两行在第 11/12 行。
- **为什么是 blocker**：**「结论对而理由错」的教科书形态**——D1 需要这两行（结论对），于是**编造了一条「契约缺行」的证据**，写进两处章节，还替它补了「两处互相矛盾」的推理。这条假断言已污染下游（催生了 B6 的「等裁决」动作），并让主 agent 为此绕了一圈。
- **建议**：删 `:283` 与 §11-C1 整节；修 `:119` 引用。

### B2 ⭐ D1 声称「`nodes` 有 20 列（`PRAGMA table_info` 已实跑）」——数字为假

- **位置**：`01-服务端与API.md:351`（`:358` 有总括背书）。
- **原文**（`:351`）：「**白名单化**：`nodes` 有 20 列（`PRAGMA table_info(nodes)` 已实跑）。」
- **实测**（`/tmp/spike1.mjs`，真 SQLite + 真 `createSchema`）：`PRAGMA table_info(nodes)` = **19**；`schema.ts:7-27` 声明 **19** 列。
- **为什么是 blocker**：纯事实，**无推理链可掩盖**，且**明标了实跑命令**。19→20 说明该实跑未发生或未核对。它同时削弱 `:358`「已跑 `PRAGMA table_info` 逐表核对」对**整节 §6.2 DTO** 的背书——而 §6.2 是全批被引用最多的中心。
- **建议**：改为 19。

### B3 ⭐ D2 要求 DTO 暴露 `anchor_*`/`first_raw_id`/`last_raw_id`，D1 明文「故意不暴露」

- **位置**：D2 `02-浏览与检索界面.md:451-452`；D1 `01-服务端与API.md:382`（DTO 定义 `:360-378`）。
- **D2 原文**（`:451`）：「`anchor_entry_id` | **详情** + **链接**：`#/raw?session=<anchor_session_id>&around=<entry_id>`」；（`:452`）：「`first_raw_id`/`last_raw_id` | **详情**：…链接 `#/raw?from=a&to=b`」。
- **D1 原文**（`:382`）：「**故意不暴露**：…`anchor_entry_id`/`anchor_session_id`（内部溯源；结论已由 `shadowed` 表达）、`first_raw_id`/`last_raw_id`…」
- **实测**：D1 的 `NodeDTO`（`:360-378`）确无这四个字段。
- **为什么是 blocker**：D2 的两个功能**按冻结 DTO 一条都做不出来**，且 D2 §9 的冲突 1–8 **没有登记它**——属于「掩盖了本该登记的未知」。两文对同一决策给出相反裁定且都不知情。
- **建议**：二选一裁决：(a) D1 加这四字段；或 (b) D2 删这两个链接并在 §9 登记不可实现。**注：§16 未覆盖本条，仍是未裁定项。**

### B4 ⭐ D5 依赖的 `current_version` 在 D1 文档里零命中

- **位置**：D5 `05-编辑与写入路径.md:187`、`:444`、`:774`；D1 `NodeDTO`（`:360-378`）。
- **原文**（D5 `:444`）：「**D1 的 `/api/node` MUST 暴露 `current_version`（含 0）**」。
- **实测**：`grep -n "current_version" 01-服务端与API.md` **零命中**。
- **为什么是 blocker**：D5 的乐观锁协议（`:187`/`:429`）全部建立在该字段上。D5 `:774` 把它登记为「待 D1 确认」，**D1 无任何对应登记**——两份文档都以为自己只是提了个无害建议，实际冻结接口缺一个被依赖的字段。
- **状态**：§16.2 已裁定 D1 加。**D1 文档落地前仍是阻塞项。**

### B5 ⭐ D6 推荐「通用 uri 粗切正则」，与契约 §8.3 的 MUST NOT 直接冲突

- **位置**：D6 `06-工程化与测试.md:391-392`；契约 `:430`、`:437`；D4 `:622-633`。
- **D6 原文**（`:392`）：「`[推断]` 稳健做法：先用 `/[\w-]+:\/\/[^\s\]）,。]+/g` **粗切**，再…最后 `resolveUri`；命不中 `resolveUri` 的候选**丢弃并计数**」。
- **契约原文**（`:430`）：「MUST **按视图的行锚点/结构解析**，**MUST NOT** 用"全文匹配 uri 正则"」；D4（`:633`）：「**「解析失败就丢弃」这条兜底不够。**…**唯一的解是按行锚点。**」
- **实测（对照 D4 SPIKE-J 反例）**：D6 的正则逐字命中被证伪的路径。D4 `:622-629` 的反例中，提取出的 `core://phantom/*` 是**正文内容**且**指向真实节点**，`resolveUri` 全部**成功** → D6 的补救（「命不中才丢」）**完全失效**。
- **为什么是 blocker**：契约与 D4 已封死并给了实测证据，D6 又作为「稳健做法」写回，还标 `[推断]`（暗示低风险）。**同批文档内对同一规则给出相反指令**，且 D6 是 parity 抽取方式的 shape owner。按 D6 写 → parity 假绿（两侧同向偏斜，契约 `:444` 描述的第二种假绿）。
- **建议**：删 `:391-392`，改引 D4 `:612-620` 的逐视图锚点表。

### B6 ⭐ D1/D4 仍以 MUST 引用契约已作废的 `MEM://` 口径（P17）

- **位置**：D1 `:283`、`:700-708`；D4 `:304`、`:311-323`；契约 `:447-468`（作废声明）。
- **D1 原文**（`:700`）：「`MEM://` 解析口径 **MUST 复用 `tools.ts:172-175` 的 `parseViewCount`**（**不得自创**）」；（`:703`）「**数字段是 limit 不是 domain**」。
- **D4 原文**（`:304`）：「`forgotten` | 5 | ⭐ **数字段是 limit 不是 domain**」；（`:322`）「`MEM://forgotten/3` → `domain=undefined, limit=3`」；（`:323`）「**必须调 `tools.ts` 的同一解析**」。
- **实测**（`/tmp/spike5.mjs` 逐字复刻 `tools.ts:184-232`；`/tmp/spike4.mjs` 用真 `renderForgottenView`）：
  ```
  MEM://forgotten / /3 / core / core/3  → domain 恒为 "forgotten"（非法）→ 全部 0 条
  MEM://timeline/core/3 → limit=20（3 被忽略）    MEM://wakeup/3 → limit=5（3 被忽略）
  ```
  根因 `"MEM://x/y".split("/")` === `["MEM:","","x","y"]` → **索引 2 是视图名本身**。
- **为什么是 blocker**：契约 §8.1 `:449` 已冻结「`/api/view` **MUST NOT 复刻 URI 解析**」，D1/D4 却用 **MUST** 写了相反要求。
- **连带风险**：D4 的 fixture 若按 `:322` 理解构造，`forgotten` 恒空 → 违反「每视图非空」→ 按 D4 §5.7 自己的矩阵，**空视图让「漏一个节点」mutation 0/6 红** → parity 对 forgotten **完全失去牙齿**。
- **建议**：两处整段替换为「显式 `name`/`domain`/`limit` 直调 `render*`」。

### B7 ⭐⭐「非空性断言」——D4 给了真东西，**D6 给的是恒真断言**（主 agent 指定最高优先级）

契约 `:475` 要求「至少一条用例必须能证明"没有这个对齐就会失败"」。**逐份核查结果：**

**(a) D4 —— 合格，且是本批唯一的真做法。**
- D4 `:706-726` 的 7 fixture × 6 mutation 矩阵，用**人为破坏结构化侧**来测灵敏度；D4 `:722` 明确写「**不是写一条特殊用例，而是保证主 fixture 每个视图都非空**，让常规断言天然有牙」。
- D4 `:1005` 的 `G1 rich` fixture 逐项列出「每视图非空」所需元素；D4 `:1006-1008` 的 G2/G3/G4 覆盖契约要求的空视图/stub/shadowed。
- D4 `:726` **主动声明**「上表的红/6 是**在我写的参照实现上**跑出的…本文没有把「预期」伪装成「已跑」」——**全批唯一一处主动划清「参照实现 / 真测试」边界**。

**(b) D6 —— 不合格。T15 在任何实现下都通过。**
- **位置**：D4 `:1046` 的 **T15**（D6 在 `:395` 只给了同一句要求）。
- **原文**（`:1046`）：「**故意构造**一个「漏一个节点」的结构化输出（`items.slice(0, -1)`），断言 parity **必须不通过**」。
- **实测**（`/tmp/spike7.mjs`）：我把该断言形式化为「把**自己构造的**结构化集合 `slice(0,-1)` 后跑 parity，断言必须 false」：
  ```
  Case A（views.ts 完全正确）: baseline parity=true → T15 得 false ✅
  Case B（views.ts 彻底坏掉、返回空集）: baseline parity=false → T15 仍得 false ✅
  ```
  **T15 在「实现完全正确」与「实现完全崩溃」两种极端下都通过。** 原因是它**自己构造并破坏**输入，与 `views.ts` 的实现**完全无关**——它不测实现，只测「`toEqual` 不是恒真函数」。
- **为什么是 blocker**：这**正是任务定义的假绿形态**——「断言在任何实现下都通过」。它给评审者一种「非空性已验证」的错觉，而真实灵敏度**只由 D4 的 T16 矩阵与 G1 fixture 提供**。若落地时 D6 被当作测试 shape owner（它自称是，`:389`），T15 会被当成「§8.3 非空性要求已满足」的证据。
- **建议**：删除或改写 T15。正确形式是**破坏实现而非破坏输出**（如 D4 T16 的矩阵：改 `views.ts` 源码里的一个过滤条件，断言 red），或至少把 T15 并入 T16 并注明「单独 T15 无灵敏度」。

**(c) D3 —— 给的不是非空性断言，而是「fixture 覆盖清单」。**
- **位置**：D3 `03-原文日志与动态区.md:744-751`、`:986`（P-4）。
- **原文**（`:744`）：「**非空性断言（契约 §8.3 要求）**：必须有一条用例证明「没有这个对齐就会红」。fixture 必须**同时包含**：…」（随后是 4 行「至少一行 XXX」）。
- **问题**：标题写「非空性断言」，正文给的是**fixture 内容清单**（`:748-751` 四行都在说「若 D4 漏掉 X，断言必须红」——是**预期**，不是**用例**）。`:986` 的 P-4 一行「手工注入缺陷后 `expect` fail」**完全无实现细节**，与 D6 的 T15 同款空壳。
- **结论**：D3 的「非空性」是**声明**而非**用例**；真正的执行体被推给 D4（D3 `:979` 自己写明 parity 文件由 D4 写）。这不是错，但 `:744` 的标题**夸大了它的兑现程度**。

**(d) D5 —— 合格（换了个场景）。**
- D5 `:741`：「T2 与 T3 是"没有对齐就会失败"的用例——若 store 哪天改成"任何 updateNode 都产 revision"，T2 立刻红」。**这是对一个真实可变的实现契约做断言**（T2 是 `updateNode({importance})` 后 `node_revisions` 行数不变）。**成立**——它测的是引擎行为，不是自造输出。

---

## ③ 应改

### S1 D3 验收用例依赖契约里不存在的 `?around=`，且 D1 少 `from`/`to`

- **位置**：D3 `:964-968`（用例 3/4/5）；契约 `:301`；D1 `:657`。
- **实测**：契约 `:301` 参数集 = `session/from/to/activeOnly/before/limit/offset`（**无 `around`**）；D1 `:657` = `session/activeOnly/limit/before`（**连 `from`/`to` 都没有**）。D3 `:258` 也把 `?around=` 当既有能力用。
- **影响**：D3 三条用例（含一条 `400` 断言）会在 D1 实现上全败。**§16.9 已裁定加 `around`；D1 漏掉的 `from`/`to` 未在任何裁定中提及。**

### S2 D6 §6.4 的 shape 表把 `wakeup` 错并入「node_id via resolveUri」

- **位置**：D6 `:386`；契约 `:438`；D4 `:509`。
- **D6 原文**（`:386`）：「`forgotten` / `recent` / **`wakeup`** | `node_id` | 文本渲染 `uri`；经 `store.resolveUri(uri)` 反查 `node_id`」。
- **契约原文**（`:438`）：「⭐ **`wakeup` 的 `## 最近动态` 段不可机械核验**…MUST 降级为**行数约束**，MUST NOT 声称能双向相等」；D4 `:509`：wakeup 是 `focuses[].node_id ∪ focuses[].children[].node_id`，「§5.3 **结构解析**」。
- **实测**：`renderWakeupView`（`memory-views.ts:124`）对 recent 段只输出 `snippet(n)`，**不含 uri** → 文本侧无从提取 node_id。
- **影响**：D6 `:386` 的写法会让实现者以为 wakeup 也能用「resolveUri 反查」做双向相等——**契约与 D4 都已否决**。且 D6 `:385-389` 的整张表是「t 文本侧抽取方式」的铺底，wakeup 那一行是错的。

### S3 S2（D2「测试：无」vs「测试：有」）——D2 `:1015` 与 `:1018-1022`

- **原文**（`:1015`）「- **本模块的自动化测试：无。**」；（`:1018`）「**⚠️ 但有一条例外，值得一个纯函数单测。**」；（`:1022`）给出 `client-logic.test.ts` 与 10 条用例。
- **影响**：`:1015` 的绝对化措辞会被当成「D2 无测试义务」的依据引用（D6 `:199` 已这样引用契约 §4.3）。**这是「测试缺席被合理化成纪律」的入口。**
- **建议**：改为「无浏览器测试；有 4 个纯函数单测（§12.2）」。

### S4 契约 §3 冻结的测试文件清单与四份下游文档实际提出的文件不一致

- **位置**：契约 `:110-113`（冻结 4 个）；D2 `:1022`、D3 `:1000`（`client-logic.test.ts`）；D5 `:719`（`write-path.test.ts`）；D6 `:376`（`views-guard.test.ts`）。
- **影响**：§3 标题即写「（冻结）」。D6 `:376` 自己写成「**或并入 parity 文件**」，D2/D3 标「`[推断]` 归属待定，请 Main 指定」——**三份文档都知道这里没定，却都按自己的写法冻结了一张表**。
- **注**：§16.3 又把 `assets/views/audit.js` 划给 D4，进一步扩大了这个未定清单。

### S5 D1 的 `:8788` 自检断言是自指的，写下即失效

- **位置**：`:119`。**原文**：「全仓扫描 `:8788` 0 命中（已实跑 `grep -rn ':8788' … plan`）」。
- **实测**：现 **10 处命中，全在该文档自身**。扫描范围含 `plan`，而文档就在 `plan` 下——**断言写下即破坏自证条件**。

### S6 D1 §12 遗留「第一版」错误映射，与紧随的「最终判据」矛盾

- **位置**：`:785-806`（第一版）vs `:812-828`（最终）；`:790`、`:1104-1108`（§11-C9）。
- **实测**：第一版 `:797`/`:799` 把冲突映射 `409 conflict`；最终 `:822`/`:824` 映射 **`400 bad_request`**。现行契约 §7.3（`:338`）已是 **7 码含 `conflict` 409**，契约变更 #8 明令改回 409。
- **影响**：D1 最终判据与现行契约**直接相反**，且第一版排在前面，极易被照抄。

### S7 D1 的 `listRevisions` 消费者计数错，而它被用作「不得改签名」的论据

- **位置**：`:421`（`:573` P6 行同）。
- **原文**：「`listRevisions` 有 4 处消费（`tools.ts:572,583` + 2 个测试，已 grep 确认）」。
- **实测**：实为 `tools.ts:572` **一处** + **3** 处测试（`test/store.test.ts:39`、`:157`、`test/tools.test.ts:59`）。**`:583` 不是 `listRevisions` 调用。**

### S8 D4 T20 建立在已被作废的 forgotten 读法上

- **位置**：D4 `:1051`。**原文**：「`forgotten` limit 语义（`tools.ts:221-225`）| …`domain="core", limit=3` === `renderForgottenView(s, "core", 3)`」。
- **实测**：引擎**不会**产出 `domain="core"`（恒为 `"forgotten"`）。该断言测的是「我期望的语义」而非「引擎实际语义」——它**会通过**，因为它拿 `renderForgottenView` 与自己比，绕开了引擎解析层。**这是一条「必过」的用例，测不出 P17。**
- **建议**：改为断言「引擎解析层与 `render*` 直调**不一致**」（即把 P17 变成回归测试），或删除。

### S9 D1 `EdgeDTO.node_id` 的注释与 `listRelated` 的 SELECT 不符

- **位置**：`01-服务端与API.md:427`。**原文**：「`node_id: string; // outgoing: 目标节点；incoming: 源节点`」。
- **实测**：`store.ts:764-783` 的**两个分支都** `SELECT ... node_id ... FROM edges`（`:768` outgoing、`:773` incoming），故 `node_id` **始终是 `edges.node_id`（边的源）**，两方向一致。主 agent 亦独立复现并更正（契约变更 #11）。
- **影响**：若实现者按该注释渲染链接，incoming 边会指向**错的节点**。

---

## ④ 建议

- **A1** D1 `:661`「超过安全整数范围在 `node:sqlite` 下不可靠（`2**53` 以上即失准）」不准。真因是 **JS `Number` 无法精确表示 > 2^53 的整数**，与 `node:sqlite` 无关；且我实测 `Number.MAX_SAFE_INTEGER > 2**53` 为 **false**（两值相等），故该刀切也不精确。
- **A2** 契约 `:377-384` 与 D1 `:984-998` 的 `data_version`「实测记录」给的是字面值 2/3。我复跑得同样 2/3（一致），但该值随库/进程历史而变。建议各加一句「数值本身无意义，仅看增减」。
- **A3** D2 `:1102`、D3 `:1000`（`client-logic.test.ts` 归属）、D5 `:774`（`current_version`）三处都在等裁决。建议主 agent 一次性广播，别让它们进实现期。
- **A4** D4 `:726` 的「诚实标注」应做成模板，推广到 D1 §10.6 与 D6 §5 那两张「实跑」表——后两张把「已跑的」与「没跑的」混在同一张表里（D1 `:1058` 的免责在全表之后，读者读到表时常已形成印象）。
- **A5** D6 `:332` 观察到「`test/web/*.ts` 的类型错误不被 `npm run check` 捕获」并登记进 §9——这是一条**真实的工程缺口**，值得保留为待拍板项，而不是被 §16 的接口裁定淹没。

---

## ⑤ 我实测 / 核对的证据清单

本机 node v26.8.1 + `node:sqlite` + 真 `MemoryStore`，脚本 `/tmp/spike{1,2,4,5,6,7}.mjs`。

| # | 被核对的断言 | 位置 | 实测 | 判定 |
|---|---|---|---|---|
| E1 | `nodes` 20 列 | D1 `:351` | `PRAGMA table_info` = **19**；`schema.ts:7-27` 19 列 | ❌ 假 |
| E2 | 契约 §7.1 缺 `/api/raw`、`/api/sessions` | D1 `:283,:1050` | 契约 `:301,:302` 均在 | ❌ 假 |
| E3 | `MEM://forgotten` 数字段是 limit | D1 `:703`、D4 `:304,:322` | `parts[2]` 恒 `"forgotten"` → **0 条** | ❌ 假（P17 已作废） |
| E4 | `MEM://timeline/core/3` 的 limit | D4 `:303` vs D1 `:702` | = **20**，`3` 被忽略 | 见 E3 |
| E5 | D6 推荐通用 uri 正则 | D6 `:392` | 与契约 `:430` MUST NOT + D4 SPIKE-J 反例冲突 | ❌ 冲突 |
| E6 | D2 需要 `anchor_*`/`first_raw_id` | D2 `:451-452` | D1 `:382` 明文不暴露，DTO 无此四字段 | ❌ 矛盾 |
| E7 | D5 需要 `current_version` | D5 `:444` | D1 全文零命中 | ❌ 缺字段（§16.2 已裁定） |
| E8 | D6 `:386` 把 wakeup 归入 node_id 反查 | D6 `:386` | 契约 `:438`+D4 `:509` 均否；`memory-views.ts:124` 只输出 `snippet` | ❌ 矛盾 |
| E9 | T15 在任何实现下通过 | D4 `:1046` | Case A（正确）与 Case B（返回空集）**都得 false** | ❌ 假绿 |
| E10 | D3 `:744` 的非空性是「用例」 | D3 `:744` | 实为 fixture 内容清单；执行体推给 D4（D3 `:979`） | ⚠️ 夸大兑现 |
| E11 | `data_version`：自写不变/他写递增 | 契约 `:349-353` | 实测 `A own write: A=2 B=3`；`B own write: A=3 B=3` | ✅ 成立 |
| E12 | 16 个 audit 事件名 | 契约 `:240` | 扫全 `src/*.ts` = **16**，名字逐一对上 | ✅ 成立 |
| E13 | `store.ts` 无 `async` 公开方法 | D1 `:226` | `^\tasync ` = **0** | ✅ 成立 |
| E14 | `renderDiagnosticView.length === 2` | D4 `:267` | 默认值截断 → **2** | ✅ 成立 |
| E15 | `renderDiagnosticView` 不接 `isVisible`、不看 shadowed | D4 `:267,:452` | `memory-views.ts:182-198` 无 `isVisible`，只 `filter(!is_stub)` | ✅ 成立 |
| E16 | `listRaw(x)` 单行精确查 | 契约 `:301`、D1 `:659` | `store.ts:924-926` `toRawId === undefined` → `raw_id = ?` | ✅ 成立 |
| E17 | `put` 同 uri 覆盖产 1 条 revision | D1 `:853,:864` | 实跑 `put×2` → `listRevisions.length === 1`，content=`V2` | ✅ 成立 |
| E18 | `snippet()` 折叠 `\s+` 且只渲染 content | 契约 `:443`、D6 `:391` | `memory-views.ts:11-14` | ✅ 成立 |
| E19 | `LIKE` 大小写不敏感、`GLOB` 敏感 | 契约 P11 `:591` | `LIKE 'TEMP://%'`→2；`GLOB`→1 | ✅ 成立 |
| E20 | `active=0` 只由 `syncRawBranch` 触发 | 契约 §6.6 `:278` | `grep "UPDATE raw_log SET active"` → 仅 `store.ts:896` | ✅ 成立 |
| E21 | `module.ts:344` 对别的会话 `continue` | 契约 §6.6 `:258` | 逐字命中 `if (currentSession && node.anchor_session_id !== currentSession) continue;` | ✅ 成立 |
| E22 | Host 正则 18 用例 | D1 `:888-903` | 复跑 19 条逐条一致 | ✅ 成立 |
| E23 | Origin 正则（端口一致、`https` 拒、尾斜杠拒） | D1 `:925-931` | 复跑 8 条全对 | ✅ 成立 |
| E24 | Pico 字节数/sha256/gzip | D6 `:158-160` | 83319 / `fbc9a63f…ba3db` / 11627 | ✅ 成立 |
| E25 | `package-lock` 443 条目 | 契约 `:144`、D6 `:241` | `Object.keys(packages).length` = **443** | ✅ 成立 |
| E26 | memory 无 LICENSE、有 README | D6 `:69` | LICENSE 不存在、README.md 存在 | ✅ 成立 |
| E27 | `listRevisions` 消费者数 | D1 `:421` | `tools.ts:572` 一处 + **3** 测试；`:583` 非调用 | ⚠️ 计数错 |
| E28 | D1 `:119` `:8788` 0 命中 | D1 `:119` | 现 10 处命中，全在该文档自身 | ⚠️ 自指失效 |
| E29 | `EdgeDTO.node_id` = 对端 | D1 `:427` | `store.ts:764-783` 两分支都取 `edges.node_id`（源） | ❌ 注释错（#11 已改） |
| E30 | `module.ts:353` `role = customType ?? role` | 契约 P5 `:572` | 逐字命中 | ✅ 成立 |

---

## ⑥ 我尝试推翻但没能推翻的断言

以下 5 条我**主动构造对抗输入**去证伪，结果**确认成立**。列出是为了给出信任刻度——**这批文档错误集中在「声称实跑却没跑」与「旧口径没同步」，不在语义推理本身。**

1. **契约 §7.4 `PRAGMA data_version` 语义**（`:349-353`）。
   两条 `DatabaseSync` + `execFileSync` 子进程写，复跑四种情形（自写不变、他写递增、跨进程递增、只读不变），**与契约表逐格一致**。这是本批**最反直觉**的一条，作者把它从「我最初写的」纠到「实测更细」，并给了可复现脚本（`:386`）。**成立。**

2. **契约 §6.6「引擎对别的会话的 auto 节点是 skip 不是 hidden」**（`:258`）。
   直读 `module.ts:334-348`：`:344` 是 `continue`，不是 `hidden.add`。若写成 hidden，UI 会把 A 会话的记忆标成「已回滚」误导使用者。**成立。** 配套论证（`active=0` 只可能由本会话造成）我单独用 `grep` 验了：全库**只有** `store.ts:896` 一处写 `active=0`。**推理链也成立。**

3. **D4「`renderDiagnosticView` 完全不看 shadowed、也不接 `isVisible`」**（D4 `:267,:452`）。
   读 `memory-views.ts:182-198` 全文：确无 `isVisible`，`nodes` 只 `filter(!is_stub)`；复跑 `.length === 2`。**成立**——意味着**任何「为 diagnostic 加可见性过滤」的好心修改都会让 parity 变红**，D4 明确写了「MUST 保留」。

4. **契约 §8.3「行锚点提取之所以安全，靠 `snippet()` 折叠空白 + 缩进到第 2 行」**（`:441-444`）。
   核了 `memory-views.ts:11-14`（`\s+ → " "`）与 `:69`（`  ${snippet(node)}` 两空格缩进），**两条实现细节都在**，`^- ` 锚点确实只能匹配列首行。**成立**——且作者把它标成「**这不是契约，是实现巧合**」并要求守卫测试（D4 T21，`:1052` 已落地）。**这是本批诚实度最高的一段：把「现在恰好安全但将来会静默变松」明确区分于「已冻结」。**

5. **D4 §5.7 的非空性矩阵与其自证边界**（`:706-726`）。
   ① **结论方向**：空视图让「漏一个节点」mutation 静默通过（`G4` 行 M1 = `0/6`），与契约 `:474` 完全吻合；② D4 `:726` 主动声明「红/6 是**在我写的参照实现上**跑出的」，**没把预期伪装成已跑**。我抽查 `/tmp/memviews-spike/`，`spike2/3/5/11/13/14/16` 描述的产物确实存在。**成立**（全批唯一一处划清「参照实现 / 真测试」边界的矩阵）。

---

## ⑦ 一句话

契约的健康度与下游文档的健康度**相反**：契约被推翻 11 次，但每次都**报错→复现→作废→登记→广播**；下游三份（D1/D4/D6）**没跟上这 11 次**，而 D1 又在两处把「没跑过的核对」写成了「已实跑」——**只看 D1 的「已实跑」清单，你会以为它整节都核过了；实际至少两项为假，且都指向同一类疏忽：数字与清单没有回头核对。** 至于「非空性断言」这一题：**D4 给了真的，D6 给了恒真的，D3 给了标题。**
