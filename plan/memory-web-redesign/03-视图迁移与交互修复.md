# 03 · 视图迁移与交互修复(九视图 → 新设计系统 + 公共层)

> **基于 00-context.md v1**。
> 范围:`packages/memory/src/web/assets/views/` 九个视图文件(tree.js / node.js / search.js / raw.js / temp.js / views.js / audit.js / databases.js / edit.js,共 5447 行,`wc -l` 实测)向 01-设计系统 + 02-公共层迁移,并落地 00-context §3 修复清单中视图侧各项:**F2、F4、F5、F6、F7、F8、F9**(F1/F3 归 02,F10 归 04)。
> 本文引用的公共件名与 02 已对齐(asyncPage / uriBreadcrumb / ui.js 零 import 判据等,见 §0.1);若 02 文档终稿改名,以 02 为准做一次全文替换即可,签名不变。

---

## 0. 迁移总纲

### 0.1 对 02 公共层的依赖契约(已与 D2CommonLayer 对齐,2026-09-18)

九个视图统一从 `ui.js`(02 的公共层,下称 `ui`)取得以下导出。**视图侧不再各自实现第二份**:

| 导出 | 签名 | 替代的现状实现 |
|---|---|---|
| `el` / `append` / `clear` | 同 app.js:97 / :115 / :123 语义 | raw.js:36、temp.js:30、views.js:52、audit.js:49、edit.js:378 五份私有 `h()` |
| `uriLine(uri, opts?)` | `opts: { href?: string, compact?: boolean }`,返回单行省略 + `title` 全文 + 点击复制的元素 | 新增(F2),替代各处裸 URI 文本 |
| `copyText(text)` | Promise,clipboard API + execCommand 兜底 | raw.js:571-589 |
| `modal.open({title, body, buttons, dismissible})` | Promise\<value\>,右上角 ×、遮罩点击可关、Esc、焦点归还 | edit.js:324-369 `openDialog` + edit.js:1644-1699 `confirmStale` 的 dialog 骨架(F1 消费方) |
| `impBadge(n)` | 四档徽章(F6) | tree.js:45-58、temp.js:60-72、views.js:85-88 三份实现 |
| `sourceBadge(source)` / `stubBadge()` / `shadowedBadge()` / `roleBadge(role)` / `eventBadge(event)` | 容错未知值,未知原样显示 | node.js:37-40、views.js:120-125、raw.js:61-75、audit.js:91-95 |
| `discBadge(text, opts)` / `discChip(text, opts)` | 语义逐字保留(entry/edge/dead/compact) | 自 views.js:95-117 迁入 |
| `breadcrumb(items, opts)` / `uriBreadcrumb({uri, domain, hrefFor?})` | 两层(D2 定名);高层内部按 URI 逐级切段 | tree.js:651-664、node.js:69-81(F7) |
| `nodeLink(uri, ctx?, label?)` | `data-nav` + 真实 href | views.js:128-133 |
| `dataTable(headers, rows)` / `pager({page, pages, onGo})` / `select(options, value, onChange)` | 基础脚手架 | audit.js:144-186、raw.js:442-476 分页、各处 select 手拼 |
| `asyncPage(host, {load, render, empty?})` | 三态(骨架/错误/空)+ mountSeq 式并发守卫,onError 可注入 app.js `renderError` | raw.js:344-369、temp.js:235-241、views.js:310-354、edit.js:956-993、edit.js:1450-1467 各自手写的 loading/error/empty 分支 |
| `foldWs` / `dash` / `num` / `formatTs` / `humanTime(ts)` / `snippet(text,max)` | 纯函数 | views.js:81-83、audit.js:79-88、node.js:54-56、temp.js:55-58、各处 `num()` |
| `splitQuery(q)` / `highlight(text, query)` / `hitExcerpt(content, terms, radius?)` | 纯函数,逐字迁移 + 新增 excerpt | search.js:21-77(F9) |
| `editorLabel(rev)` | 统一版(02 负责两份语义 diff 后收敛) | edit.js:211-220 与 node.js:22-35 两份不一致实现 |

**ui.js 的硬约束(D2 已升级采纳)**:零 import、零顶层 `document`/`localStorage`/`window` 访问;判据 `node -e "import('./views/ui.js')"` 不抛;`ui.el` 只用 `createElement/createTextNode/className/textContent/dataset/setAttribute/append/addEventListener` 最小 Node API——这保证 `disc-badge.test.js` 的假 DOM(Node + `document.createElement/createTextNode`)能继续真实驱动被测模块(§12.4)。

### 0.2 迁移中不得破坏的冻结项(逐条对照 00-context §2,标注落点)

1. **绝不 innerHTML**:九视图现在就全走 `textContent`/DOM API;迁移后由 `ui.el` 继续保证。新组件(uriLine/badge 等)一律不经 HTML 串。
2. **读路径零 POST**:tree/search/raw/temp/views/audit/databases(读)照旧;写只出自 edit.js 的写原语(edit.js:31-205)。
3. **edit.js 无裸 `fetch(`**(哨兵 V7a):迁移后仍 `import { get, post } from "../app.js"`(哨兵 V7b 的正则钉死了这个 import 形状,D2 已确认字面保留,**不挪进 ui**)。
4. **url.js / tree-cache.js 不动**:这两个纯模块不在九视图之列,一行不改。
5. **事件契约**:`memory:changed` 仍只有 edit.js 的 `notifyChanged`(edit.js:41-43)派发;`mw:toast` 仍走 `ctx.toast`(已知 toast 派发/监听错位的 bug 由 02 修,app.js:251-253 派发于 `document`、app.js:765 监听挂在 `#mw-toasts` 后代导致事件不达——视图侧调用方式不变,依赖 02 修复后验收,见 §15)。
6. **多库 N+1 禁令**:databases.js 一页一次 `/api/databases`;**temp.js 的 enrich 逐条 `/api/node`(temp.js:148-175,CAP=60)是既有妥协,本次保留不动**(§15 记录为已知债务,不在本次范围)。
7. **`?db=`**:全部经 `ctx.api`,视图零感知。
8. **路由 / parseHash / navigate**:不动(app.js:229-248);`peek` 的 `history.replaceState`(tree.js:555-563)语义保留。
9. **mw- CSS 前缀**:新类名继续 `mw-` 开头。
10. **哨兵 V11c/V11d**:tree.js 迁移后仍 `import … from "./tree-cache.js"`、不得出现 `export const treeCache`;`loadLayer` 的 `const gen = currentGen()` 必须仍在 `await` 之前、`if (gen !== currentGen())` 仍在 `await` 之后(multi-db-frontend.test.ts:127-147 按顺序检查源码文本)。**迁移 tree.js 时这两段逐字搬运。**

### 0.3 语义冻结清单(迁移时禁止"顺手改"的行为清单)

这些是各视图现測正确、且被测试或契约钉住的行为。迁移 PR 的 diff 里它们应当**只出现在 import 行与布局类名上**,逻辑行保持逐字:

| 行为 | 位置 | 冻结理由 |
|---|---|---|
| 树缓存失效点:`ensureCacheFor(ctx.currentDb)` 必须是 mount/mountSidebar 第一句 | tree.js:601、tree.js:669 | 多库读 A 读 B 防线(V11 系列) |
| `loadLayer` 竞态守卫:gen 前抓后验,旧库响应丢弃且不写缓存 | tree.js:99-122 | V11d 哨兵 + 切库污染防线 |
| 树状态回执:被过滤条数必须显式写出,从不静默过滤 | tree.js:569-581 | 「从不静默过滤」纪律 |
| 搜索竞态:回包序号 ≠ 当前序号则丢弃;卸载 `seq++` | search.js:186-188、search.js:260-263 | 慢请求不得覆盖新结果 |
| IME 守卫:`isComposing || keyCode === 229` 不当查询 | search.js:236-243、app.js:756-761 | 中文用户"搜不出东西"的主因(P20) |
| raw 深链必须同时带 session(`entry_id` 跨会话重名 → 服务端 bad_request) | raw.js:561-564、node.js 锚点深链 | 契约 §16.14 |
| raw 措辞:「已切走(不在当前分支)」,不说已隐藏/已删除 | raw.js:24、raw.js:388 | 契约 §6.6 文案纪律 |
| shadowed 文案:「已遮蔽(原分支已回滚)」 | tree.js:27-28、views.js:120-125 | 契约冻结 + 广播 |
| timeline 与 `#/raw` 同源同口径(raw_log + active=1) | raw.js 页脚说明 | §8.3 双端点互验 |
| views.js 只渲染 DTO、不重算视图语义;`MEM://` name 取固定枚举 | views.js:12-21、views.js:142-147 | 契约 §16.8 / P17 |
| wakeup 最近动态按 uri 去重(显示层,不改 DTO) | views.js:467-476 | DTO 含重复的既定处置 |
| audit 零 POST、永不派发 `memory:changed`、不做聚合/图表/告警 | audit.js 文件头、audit.js:188-223 | §1.3 红线(决策 23) |
| audit 未知事件名/未知 role 原样显示 | audit.js:91-95、raw.js:61-75 | 契约 §7.4 customType 纪律 |
| edit 写原语签名(createNode/reviseNode/forgetNode/restore/relocate/addEdge/addGlossary/removeGlossary/awaken/setWorldTime) | edit.js:55-205 | D5 §10.2 签名冻结;`relocate` 只发 `{moves}` |
| `version === 0` 是合法成功,三分支文案 | edit.js:121-132 | 契约 §16.10b |
| 乐观锁三选(放弃/覆盖/看差异) | edit.js:625-655、edit.js:1644-1699 | 契约 §6.2 |
| entry-scope 写入:`runSaveMeta` 用 `state.uri` 而非 `node.uri` | edit.js:703-722 | T4+T5:别名入口条件写别名层 |
| `field("…")` 读值 + `data-field`/`data-action`/`data-form` 委托机制 | edit.js:580-594 等 | 迁移保持事件委托形态(重渲染不泄漏) |
| 恢复页按 node_id 分组、只对最新 URI 开放恢复、父先子后排序 | edit.js:1370-1606 | P14 陷阱(静默拿回旧正文) |
| countSubtreeSize 的保守降级(depth=3 数不到就少报) | edit.js:1621-1633 | 「至少 N 条」的诚实性 |
| databases 三端点不带 `?db=`、不施放 `memory:changed` | databases.js 文件头、databases.js:262-312 | 契约 §3.4 / §6 |
| 新建库必须先 confirm(databases.js:239-241 复用 confirmSimple) | databases.js:232-253 | 契约 §5.3 |

### 0.4 每个视图的统一机械步骤

1. **换血 import**:删本地 `h()/paramOf()/toast()/navigate()/num()/…`,改 `import { … } from "../ui.js"`;需要 ctx 的经 `ctx.api/ctx.toast/ctx.navigate`(ctx 已具备,app.js:311-343)。
2. **布局类名对齐 01**:外层容器、行、徽章、表单换 01 的 token 化类(保留 `mw-` 前缀与既有 BEM 语义名,如 `mw-row`/`mw-chip` 不改名只换皮)。
3. **修复项落地**(该视图承担的 F 项,见 §10)。
4. **删死代码**:被 ui 吸收的本地实现整段删除(clean cutover,不留 re-export shim;`editorLabel` 过渡期 re-export 是 02 文档裁定的唯一例外)。
5. **手工验收**(§13 的对应条目)。

---

## 1. tree.js(784 行)

### 1.1 现状结构

- 头部注释与依赖:tree.js:1-23;import app.js 助手(:16)、tree-cache 纯模块(:17)、**views.js 的 `discChip`(:22,零环方向注释 :18-21)**。
- 常量:`PAGE_SIZE=200`/`WINDOW_THRESHOLD=500`/`WINDOW_BUFFER=5`(:31-33);`SHADOWED_TEXT/TITLE`、`FILTER_LABEL`(:27-29);`DOMAIN_ORDER`(:31);`VIEW_LINKS` 七系统视图(:34-42)。
- 纯展示助手(导出给 node/search):`importanceClass`(:45-53)、`impChip`(:55-58)、`shadowedChip`(:60-62)、`lastSegment`(:64-68)、`ancestorChain`(:70-77)、`domainRank`/`sortDomains`(:79-92)。
- 数据层:`loadLayer`(:99-122,缓存+gen 守卫);树状态 `createTree`(:124-150)、`flattened`(:153-177)、`droppedCount`(:179-186)。
- 行渲染与窗口化:`buildRow`(:193-298)、`rowHeight`(:295-299 读 `--mw-row-h`)、`buildTreeHost`(:301-330)、`applyWindow`(:332-358)、`paint`(:360-394)、`moreBox`(:396-415)、`loadMore`(:417-430)、`attachScroll`(:432-448)、`ensureVisible`(:450-461)。
- 键盘导航:`attachKeyHandler`(:464-500,WAI-ARIA tree 方向键)、`focusRow`(:502-516,roving tabindex)。
- 展开/懒加载:`toggle`(:518-539)、`expandAncestors`(:541-553)。
- 抽屉 peek:`peek`(:555-566,replaceState + 动态 import node.js)。
- 状态与过滤:`renderStatus`(:569-581)、`filterToggle`(:583-597)。
- 主区:`mount`(:600-648);`breadcrumb`(:651-664)。
- 侧栏:`mountSidebar`(:668-784,域选择器 :686-712、过滤 :714-721、系统视图链接 :727-739、meta 计数 :741-747、新建入口 :749-771)。

### 1.2 迁移后结构

```
views/tree.js(目标 ~660-690 行)
  import ui(el/append/clear/impBadge/discChip/stubBadge/shadowedBadge/uriLine/uriBreadcrumb/nodeLink…)
  import tree-cache(不动)
  ── 常量区:PAGE_SIZE/WINDOW_*/SHADOWED_*/FILTER_LABEL/DOMAIN_ORDER/VIEW_LINKS(逐字保留)
  ── 树核心:loadLayer(逐字保留,含 gen 守卫)/createTree/flattened/droppedCount
  ── 行渲染:buildRow(F8 重构,§10.7)/buildTreeHost/applyWindow/paint/moreBox/loadMore/attachScroll/ensureVisible
  ── 键盘:attachKeyHandler/focusRow(逐字保留)
  ── toggle/expandAncestors/peek(逐字保留)
  ── renderStatus/filterToggle(逐字保留)
  ── mount / mountSidebar(uriBreadcrumb + ui 组件重组)
  删除:importanceClass/impChip/shadowedChip/lastSegment/ancestorChain/breadcrumb 本地实现(→ ui;
        lastSegment/ancestorChain 若 ui 未收,保留为本文件私有并同步知会 02——默认 ui 收)
```

纯展示助手迁出后,**tree.js 不再被 node.js/search.js 当作助手来源**(它们改从 ui 取);tree.js 对 views.js 的 import(:22)随之消失,原"防环"注释(:18-21)整段删除——discChip 改从零 import 的 ui 取,环风险根除。

### 1.3 用的公共件

`el/append/clear`、`impBadge`(F6)、`discChip`、`stubBadge`(F6)、`shadowedBadge`、`uriLine`(F2,compact 形态)、`uriBreadcrumb`(F7)、`select`(域选择器,tree.js:686-701 手拼的下拉)、`asyncPage`(mount/mountSidebar 的 meta 拉取失败分支,tree.js:611-614、670-676)。

### 1.4 DOM/交互变化

- **F8 行重构**(§10.7):`buildRow` 的 `mw-uri` 内部拆 `mw-uri-prefix`(弱化等宽前缀)+ `mw-uri-seg`(主段名),新增 child_count 徽标;caret/↗/roving tabindex/aria-label 结构不变。
- **F2**:侧栏 compact 行的 URI 用 `uriLine(uri, {compact:true})`(点击复制;单击行仍 peek)。
- **F6**:tail 徽章换 `impBadge/discChip/stubBadge/shadowedBadge`;`●`/`◌` 裸符号并入 stub 徽章的视觉。
- **F7**:主区面包屑(tree.js:651-664)换 `uriBreadcrumb`,默认 hrefFor 指向 `#/tree?domain=…&uri=…`(保持现有语义:树内回溯),段名 = `lastSegment`。
- 侧栏视觉重组(分组标题、计数徽章化)归 01 皮,结构不变:新建入口、域选择、过滤开关、系统视图链接、meta 计数、世界时间的**顺序与语义全保留**(tree.js:752-771 的组装次序)。

### 1.5 行数预估

784 → **约 660-690**(-12%~-16%)。构成:纯助手迁出 -50;import/注释净 -10;F8 行重构 ±10;面包屑换组件 -8;侧栏 select/脚手架换 ui -15。**窗口化/键盘/缓存三块(~330 行)是行为承重墙,不减。**

### 1.6 验收路径(浏览器)

`#/tree`:展开/折叠、←/→/↑/↓/Enter 键盘、>500 行域的窗口化滚动(示例库 index 域不触发,可用 `--db` 指向大库)、过滤开关勾选后的「已按分支可见性过滤掉 N 条」回执、「载入更多」、单击行 peek → Esc 关抽屉焦点回树行、↗ 全页、面包屑逐级可点、行内前缀弱化 + 段名完整、child_count 显示。写一条记忆后侧栏与主区同步失效刷新(`memory:changed` → `invalidateTreeFor`)。

---

## 2. node.js(480 行)

### 2.1 现状结构

- 头部三条契约点注释(node.js:1-19);import tree.js 助手(:20)+ views.js 徽章(:21)。
- `editorLabel`(:22-35,自称"唯一署名渲染点",与 edit.js:211-220 **语义不一致**:model 显示为裸 id vs `By ${model}`,auto 显示「自动写入(模型未知)」vs edit 版回落 source 字符串)。
- `sourceChip`(:37-40)、`kv`(:42-51)、`dash`(:54-56)、`section`(:58-66)。
- `renderBreadcrumb`(:69-81,**现状:每个祖先渲染完整 URI 文本**,长链撑爆一行)。
- `renderRevisions`(:85-135,含"恢复到此版本"按钮位 + 事件委托说明)、`renderAliases`(:137-152)、`renderGlossary`(:154-171)、`edgeList`(:173-213,dangling/旧地址/边条件 chip)与 `renderEdges`(:215-232)。
- `renderMeta`(:234-253)——**现状是死渲染 bug**:`details` 构建后从未 append 进 DOM,只 append 了一个空 `div`(:251-253),「技术信息」折叠区实际上从不显示。
- `renderNode`(:256-376,头部徽章、写入口按钮 :270-289、shadowed 提示、想起条件、正文/占位、子节点、anchor 深链 :339-348、页脚)。
- `renderEntryDisclosures`(:377-426,入口条件区块,死别名「被遮蔽(同名节点优先)」)。
- `mount`(:429-448)、抽屉 `openDrawer`(:450-466)/`onDrawerClose`(:468-475,焦点归还)/`closeDrawer`(:477-480)。

### 2.2 迁移后结构

```
views/node.js(目标 ~380-410 行)
  import ui(全部徽章/kv/section/uriBreadcrumb/uriLine/nodeLink/editorLabel 统一版)
  import tree.js:无(改从 ui 取 impChip 等)
  ── editorLabel:过渡期 re-export ui 版(02 裁定),随后删除
  ── renderBreadcrumb:换 uriBreadcrumb(F7)
  ── renderRevisions/renderAliases/renderGlossary/renderEdges/renderEntryDisclosures:徽章化 + dataTable 化,逻辑逐字
  ── renderMeta:接线修复(把 details append 进 DOM)
  ── renderNode / mount / openDrawer / onDrawerClose / closeDrawer:结构保留
```

### 2.3 用的公共件

`impBadge/sourceBadge/stubBadge/shadowedBadge/discBadge/discChip`、`uriLine`(头部 URI,F2)、`uriBreadcrumb`(F7)、`kv`、`section`、`dataTable`(revisions 表)、`editorLabel`(统一版)、`nodeLink`(子节点/边列表)。

### 2.4 DOM/交互变化

- **F7**:面包屑改为 `domain://a/b/c` 逐级段名可点(链到 `#/tree?uri=<祖先完整uri>`,现状 :73 链接目标保持),当前节点 `aria-current` + `title` 全 URI + 点击复制(uriLine 形态)。数据源仍用 DTO `path`(注释 :67-68 的"不自己切 /"纪律保留)。
- **F2**:页头 `<h2>` 里的裸 URI(node.js:265)换 `uriLine`(等宽、省略、title、点击复制 + toast)。
- **F6**:`sourceChip`→`sourceBadge`(manual/auto/import 色阶);子节点行的 imp/stub/shadowed 徽章化;边 `kind` 裸 chip 保留文本但走徽章底座。
- **renderMeta 接线**(Main 已拍板):`details` 挂进 `el_`;顺手把 kv 值中的 `anchor_session_id`/`anchor_entry_id` 换成 `uriLine`(等宽可复制,同时满足 F2 的"UUID 仅详情中等宽展示"精神)。
- 深链「跳转到产生它的原文」(:339-348)行为不变;其说明行的裸 session UUID(:346)换 `uriLine`。

### 2.5 行数预估

480 → **约 380-410**(-15%~-21%)。editorLabel/kv/dash/section 迁出 -30;revisions 表换 dataTable -12;徽章化净 -10;renderMeta 修复 +3;F7/F2 重写 ±0。

### 2.6 验收路径

从树点开节点(抽屉)与 `#/node?uri=…`(全页)两路:面包屑逐级跳树、URI 点击复制(toast 依赖 02 修复,见 §15)、出边/入边/悬空边/旧地址标记、入口条件区块(多入口节点)、触发词、历史版本「查看」与恢复按钮位、技术信息折叠区**现在应该能展开看到**(回归此 bug 修复)、anchor 深链跳 `#/raw`。

---

## 3. search.js(279 行)

### 3.1 现状结构

- `splitQuery`(:21-27,CJK 段 + 拉丁词,导出供单测)、`highlight`(:32-77,indexOf 循环 + 区间合并 + `<mark>`,全 textContent)。
- `renderCard`(:80-129,标题链接 + imp/source chip、**summary 已做高亮**(:89-91)、discBadge、meta 行、诊断折叠 kw/vec/bm25 + 触发词命中的诚实边界提示(:120-124))。
- `renderModeHint`(:131-134)、`renderResults`(:136-144)、`renderEmpty`(:147-159,域过滤与否两分支)。
- `mount`(:162-279:防抖 350ms、竞态 seq、IME 守卫、Enter 写 hash、域/条数选择器、错误卡 renderError)。

### 3.2 迁移后结构

```
views/search.js(目标 ~160-180 行)
  import ui(splitQuery/highlight/hitExcerpt/impBadge/sourceBadge/discBadge/uriLine/select/asyncPage)
  ── renderCard(F2 标题、F6 徽章、F9 命中窗口)
  ── renderModeHint/renderResults/renderEmpty(逐字,皮换)
  ── mount(逐字:防抖/竞态/IME/hash 深链)
  删除:splitQuery/highlight 本体(→ ui,导出性质不变,单测可继续打 ui)
```

### 3.3 用的公共件

`splitQuery/highlight/hitExcerpt`(F9)、`impBadge/sourceBadge/discBadge`(F6)、`uriLine`(F2)、`select`(limit/domain 两个手拼下拉)、`asyncPage`(首屏 note/results 的装载分支)。

### 3.4 DOM/交互变化

- **F2**:结果卡标题链接(现状 :83 裸 URI 文本)换 `uriLine(uri, {href})`——点击复制、中键/右键保留链接语义;等宽 + 单行省略 + title 全文(现状 CSS 是 `word-break: break-all` 折行,app.css:116,废弃)。
- **F6**:importance/source 徽章化。
- **F9**:见 §10.8(摘要命中窗口增强)。
- 空结果、域过滤、条数、诊断折叠的**行为与文案逐字保留**。

### 3.5 行数预估

279 → **约 160-180**(-36%~-43%)。splitQuery+highlight 迁出 -57;renderCard 换组件 -15;mount 换 select/asyncPage -20;F9 增强 +12。

### 3.6 验收路径

`#/search?q=薇拉`:中文词命中高亮(mark 样式)、命中点在正文深处时卡片出现「…命中窗口…」摘录(F9 新行为)、触发词命中(正文无该词)时诚实提示仍在、`kw=0` 无高亮不报错、域过滤/条数、IME 组字中回车不触发、慢请求不覆盖新结果、Enter 后 hash 可复制。

---

## 4. raw.js(590 行)

### 4.1 现状结构

- 常量:`PAGE_SIZE=50`(:14)、`KNOWN_ROLES`(:16)、`INACTIVE_TITLE`(:19-22)、`INACTIVE_CHIP`(:24,措辞冻结)。
- 工具:`paramOf`(:29-33)、`h`(:36-58)、`toast`/`navigate` 局部包装、`roleBadge`(:61-75)、`activeStyle`(:78-80)。
- `mount`(:84-519):事件委托 onClick(:176)/onChange(:223)/onSubmit(:238);取数 `boot`(:122-134,`/api/sessions` + 全库 active 探针)/`load`(:136-174,from/to/around/before 三选一)。
- 渲染:`render`(:252)、`renderHeader`(:256-342,**session 下拉选项 `sessionOptionLabel(s)`(:268、:547-550)带完整 UUID;`from raw_id`/`to raw_id` 输入(:298-311)、正倒序切换**)、`renderBody`(:344-369)、`renderRow`(:372-439,**行头裸 `row.session_id`(:392)、entry_id 复制按钮 + 以它为锚点**、详情 dl 含 session_id(:419-420))、`renderFooter`(:442-506,keyset 分页 + 过滤回执 + 与 MEM://timeline 同源说明)、`inactiveCount`(:508-517)。
- 纯函数导出:`sortItems`(:521)/`filterByRole`(:527)/`minRawId`(:532);`sessionOptionLabel`(:547)、`short`(:552)、`rawDeepLink`(:561)、`copyText`(:571-589)。

### 4.2 迁移后结构

```
views/raw.js(目标 ~480-510 行)
  import ui(el/copyText/uriLine/roleBadge/eventBadge 底座?否——roleBadge 迁 ui/select/pager/humanTime/asyncPage)
  ── state/事件委托/取数:逐字保留(from→around 语义、activeOnly、role 本页过滤)
  ── renderHeader:F4 重做(F4 主方案 = 服务端 first_text,§10.3;A 档降级)
  ── renderRow:去行头 UUID、entry_id 换 uriLine、detail 保留等宽 UUID(F4)
  ── renderFooter:ui.pager + 回执逐字
  ── 纯函数导出保留:sortItems/filterByRole/minRawId + 新增 parseLocateInput(§10.3,供单测)
  删除:h/paramOf/toast/navigate/copyText/short/sessionOptionLabel 旧实现
```

### 4.3 用的公共件

`el/append/clear`、`copyText`(F2 共用)、`uriLine`(entry_id/session_id)、`roleBadge`(迁 ui)、`select`(session/role 两个下拉)、`pager`(keyset 分页按钮组)、`humanTime`(wall_first/wall_last 格式化)、`asyncPage`(错误卡/骨架分支)、`formatTs`(详情 dl 时间)。

### 4.4 DOM/交互变化

全部 F4,详见 §10.3:定位输入(`#123`/`123`)替代 from/to 裸参数名;session 下拉人类标签(起始时间 + 条数 + 活跃 + 首条摘要);行头去 UUID;UUID 仅详情等宽。正倒序、activeOnly 开关、role 本页过滤、分页、回执措辞全部不动。

### 4.5 行数预估

590 → **约 480-510**(-14%~-19%)。h/paramOf/toast/navigate/copyText/short -55;select/pager/asyncPage -35;F4 重做 header/row ±15(含 parseLocateInput +12)。

### 4.6 验收路径

`#/raw`:定位输入 `#30` → 落到 raw_id 30 起始页;`#abc` 行内报错;session 下拉显示「09-15 08:30 · 42 条 · 活跃 40 · 商队从北方来…」人类标签,选中后过滤生效;行头无 UUID、详情里等宽 UUID 可复制;「已切走」行斜纹样式与悬停说明;reroll 库(示例库 s1 有 active=0 行)勾掉「只显示当前分支」→ 回执「已按分支过滤掉 N 条」;分页键控正确(«较新一页/较早一页»);与 `#/view?name=timeline` 对照行数一致。

---

## 5. temp.js(420 行)

### 5.1 现状结构

- 工具:`h`(:30-52)、`snippet`(:55-58)、`impClass`(:60-72,**与 tree.js 版本分叉:多一个永不命中的 `v>=6` 分支**)、`calibrateStage`(:76-85,导出供单测)、`num`(:417-420)。
- `mount`(:88-113):`boot`(:115-145,stub 双重排除)、`enrich`(:148-175,**逐条 `/api/node` 补 created_at/source,CAP=60、并发 6,失败降级不抛**)、事件(:177-206)、`render`(:210)、`renderHeader`(:221)、`renderGauge`(:235-283,进度条/四态 stage 文案/阈值来源)、`renderNotifyPreview`(:285-291)、`renderList`(:293-335)、`renderRow`(:337-396,归位/删除/查看三按钮)、`sortNodes`(:400-415,导出供单测)。

### 5.2 迁移后结构

```
views/temp.js(目标 ~330-355 行)
  import ui(el/impBadge/sourceBadge/shadowedBadge/uriLine/select/asyncPage/formatTs)
  ── calibrateStage/sortNodes:逐字保留(单测锚点)
  ── enrich:逐字保留(含 CAP/并发/降级注释)
  ── renderHeader/renderGauge/renderNotifyPreview/renderList/renderRow:徽章化 + 皮换
  删除:h/snippet/impClass/num 本地实现
```

### 5.3 用的公共件

`impBadge`(F6,统一四档)、`sourceBadge`(enrich 后有 source 列)、`shadowedBadge`(temp.js:355-361 现为手写 chip+文案)、`uriLine`(行 URI,F2)、`select`(排序)、`asyncPage`(错误卡)、`formatTs`(「放入于 …」)。

### 5.4 DOM/交互变化

- F6:`★N` 裸文本(temp.js:341)与 source chip(:344)换徽章;shadowed 文案沿用冻结句(不换措辞,只换底座)。
- F2:行 URI 换 `uriLine`(现状 :342 裸 a 链接全文折行)。
- 「归位/删除/查看」三按钮与跳转(`#/edit?uri=…&action=forget` 的进页即确认)不动。
- enrich 的 N+1 保留(§0.2 第 6 条)。

### 5.5 行数预估

420 → **约 330-355**(-16%~-21%)。h/snippet/impClass/num -45;select/asyncPage -20;徽章化 -10。

### 5.6 验收路径

`#/temp`:空库态「清到零 ✅」;有草稿时进度条与 stage 文案(阈值来源三态);排序切换;enrich 后 created_at/source 列出现;「归位」进编辑页、「删除」进编辑页并弹高危确认;已遮蔽草稿不计入触发的说明行。

---

## 6. views.js(657 行)

### 6.1 现状结构

- 常量:`VIEWS`(:12-20)、`EMPTY_TEXT`(:23-31)、`VIEW_NOTES`(:34-42)。
- 工具:`paramOf`、`h`(:52-77)、`foldWs`(:81-83,导出)、`stars`(:85-88,**无档位 class 的裸 `★N`**)、`discBadge`/`discChip`(:95-117,**全站徽章视觉真相源,将被 ui 收编**)、`shadowedBadge`(:120-125)、`nodeLink`(:128-133)、`formatUpdated`(:136-138)。
- `mount`(:142-167)+ `boot`/`load`(:169-201)+ 事件(:204-233)。
- 渲染:`render`(:237)、`renderTabs`(:247-265)、`renderControls`(:267-308)、`renderBody`(:310-354,switch 七分支)。
- 七视图渲染:`renderTimeline`(:356-372)、`renderForgotten`(:373-393)、`renderRecent`(:395-411)、`renderIndex`(:413-436)、`renderGlossary`(:438-457)、`renderWakeup`(:459-559,焦点/跳过/最近动态三段 + 去重)、`renderDiagnostic`(:561-645,三分类清单)。
- `skipReasonLabel`(:647-653)。

### 6.2 迁移后结构

```
views/views.js(目标 ~460-500 行)
  import ui(全部徽章/uriLine/dataTable/select/foldWs/nodeLink/asyncPage)
  特性:本文件回归"零 import app.js" purity 不再必要(discBadge 迁 ui 后无人再借道它取徽章),
        但仍保持零顶层副作用(disc-badge.test.js 假 DOM 迁移后打 ui,本文件不再被测试 import)
  ── VIEWS/EMPTY_TEXT/VIEW_NOTES:逐字保留
  ── 七个 render*:只渲染 DTO 的纪律逐字保留;脚手架换 ui(list/table/card)
  删除:h/foldWs/stars/discBadge/discChip/shadowedBadge/nodeLink/formatUpdated 本地实现
```

### 6.3 用的公共件

`impBadge`(替换 `stars`)、`stubBadge`(renderGlossary :454 的 `占位` small、diagnostic 占位分类)、`shadowedBadge`、`discBadge/discChip`、`uriLine`(renderForgotten/renderRecent/renderIndex/renderWakeup/renderDiagnostic 的裸 URI 链接,**含 :531/:621 的 `<code>` 裸 URI**)、`dataTable`(renderGlossary :438-457 的手拼表)、`select`(域/条数)、`asyncPage`(renderBody 的 loading/error 前置分支 :310-330)。

### 6.4 DOM/交互变化

- F2:全页所有节点 URI 链接换 `uriLine(uri, {href})`;skipped 列表(:531)与 diagnostic 占位列表(:621)的 `<code>` 同样处理。
- F6:`stars` → `impBadge`(这是**行为升级**:renderForgotten/renderRecent/renderWakeup/renderDiagnostic 的 ★ 从此有档位视觉);glossary「占位」与 diagnostic 占位行换 `stubBadge`。
- F7:无(本页无面包屑挂点)。
- 七视图语义、空态文案、limit 1-200 校验、domain 下拉、total 回显逐字保留。

### 6.5 行数预估

657 → **约 460-500**(-24%~-30%)。h/foldWs/stars/discBadge×2/shadowedBadge/nodeLink 迁出 -75;dataTable/select/asyncPage -45;七渲染脚手架(list/ol/li/section 样板)收紧 -60;徽章/uriLine 替换 ±0。

### 6.6 验收路径

`#/view?name=` 七个 tab 逐一:timeline 行序与 `#/raw` 对照(active=1、倒序)、forgotten 天数卡、wakeup 焦点卡 + 子列表 + skipped 折叠 + 最近动态去重、glossary 表(触发词→指向)、recent 修改时间、index 分域根清单、diagnostic 三分类(示例库 core 域有 crowded 数据)。URI 全部等宽省略 + 点击复制。

---

## 7. audit.js(225 行)

### 7.1 现状结构

- `EVENT_CLASS` 16 事件 → 9 配色组(:22-45,未知走 `mk-unknown`)。
- 工具:`h`(:49-72,含 onclick 陷阱注释)、`clear`(:74-76)、`formatTs`(:79-82)、`foldWs`(:85-88)、`eventBadge`(:91-95)、`detailsText`(:98-108)。
- 渲染:`renderRow`(:113-142,**对象列裸 URI 链接、细节列 80 字截断 + title 全文**)、`renderList`(:144-160)、`renderPager`(:163-185)。
- `mount`(:188-223,limit/offset 参数、零 POST、dispose 空实现但必须返回)。

### 7.2 迁移后结构

```
views/audit.js(目标 ~150-165 行)
  import ui(el/dataTable/pager/eventBadge/uriLine/asyncPage/foldWs/formatTs)
  ── EVENT_CLASS:逐字保留(配色组语义)
  ── renderRow:对象列换 uriLine(uri,{href})(F2);其余列逐字
  ── mount:逐字(limit/offset 校验、错误分支、空 dispose 返回)
  删除:h/clear/formatTs/foldWs/eventBadge/renderList 手拼表/renderPager
```

### 7.3 用的公共件

`dataTable`(表 + 固定列宽皮归 01)、`pager`、`eventBadge`(迁 ui,`mk-*` 配色类名保留给 01 上 token)、`uriLine`(对象列)、`asyncPage`(错误分支 :213-219)、`formatTs/foldWs`。

### 7.4 DOM/交互变化

- F2:对象 URI 等宽 + 省略 + title + 点击复制(现状 `mw-audit` 表已做单行省略 app.css:226-230,JS 侧 details 列的 80 字截断保留——两种截断语义不同,**对象列的 CSS 省略不变,URI 复制新增**)。
- 分页/事件徽章/只读纪律/「细节」列截断全部不动。

### 7.5 行数预估

225 → **约 150-165**(-27%~-33%)。h/clear/foldWs/formatTs/eventBadge -35;dataTable/pager -30;uriLine ±2。

### 7.6 验收路径

`#/audit`:翻页、事件徽章九组配色、对象 URI 点击复制、细节列悬停 title 全文、错误注入(改 limit=abc)落参数缺省;读前后 audit_log 行数不变(零写)。

---

## 8. databases.js(313 行)

### 8.1 现状结构

- `load`(:28-38,403/404 走 renderNotice 说明分支的语义)、`num/txt`(:41-47)、`pathCell`(:50-52,**`.mw-db-path` 的 `word-break: break-all`(app.css:252)是"路径逐字符竖排折行"的直接原因**)、`table`(:54-66)。
- `renderSummary`(:68-80)、`renderTable`(:82-115,六列:label/路径/节点数/世界时间/状态/切换按钮;`(进程库)` 标记 :90)、`renderManualOpen`(:118-140)、`renderCreate`(:142-163)、`renderDiagnostic`(:165-186)、`renderAll`(:188-198)。
- 动作:`showInlineError`(:201-206)、`describeErr`(:209-212)、`doOpen`(:215-230)、`doCreate`(:232-253,confirmSimple)、`doSwitch`(:255-258)。
- `mount`(:262-312,事件委托 + dispose)。

### 8.2 迁移后结构

```
views/databases.js(目标 ~250-270 行)
  import ui(el/dataTable/uriLine/badge 系/confirm(ui 版))
  ── load/renderSummary/renderDiagnostic:逐字
  ── renderTable:F5 重排(§10.4)
  ── renderManualOpen/renderCreate:表单皮换,confirm 调用换 ui 版
  删除:num/txt/pathCell 旧实现、table() 手拼(→ dataTable)
```

### 8.3 用的公共件

`uriLine`(路径格,F5)、`dataTable`、`badge`(进程库标记、可达/不可达状态,F6 底座)、`confirm`(02 modal 的简单确认,替代 edit.js confirmSimple import——**databases.js 对 edit.js 的唯一 import(:22)随之消失,模块图更干净**)、`renderNotice/renderError`(继续用 app.js 的)。

### 8.4 DOM/交互变化

= **F5**,详见 §10.4。行为(切换/打开/新建/诊断、N+1 禁令、不带 `?db=`、不施放 `memory:changed`)逐字不动。

### 8.5 行数预估

313 → **约 250-270**(-14%~-20%)。table()/num/txt -20;表单/诊断皮换 -15;F5 重排 ±5;confirm 换源 -6。

### 8.6 验收路径

`#/databases`:路径列等宽单行省略、悬停 title 全文、点击复制;窄窗口下拉到 300px 宽行不逐字折行;「切到此库」可达/禁用态;不可达行的虚线边 + 错误 title;手动打开一个错误路径 → 表单下方就地错误;新建库弹确认(× 可关、遮罩可关、Esc 取消——顺带验收 02 的 F1);403/404 语义分支(说明而非报错)。

---

## 9. edit.js(1699 行)

### 9.1 现状结构

- **写原语区(:31-205,签名冻结)**:`postJSON/getJSON`(:31-38)、`notifyChanged`(:41-43)、`currentRoute`(:45-51)、`createNode`(:55-73)、`reviseNode`(:76-119,乐观锁)、`describeReviseOutcome`(:121-132)、`forgetNode`(:134-139)、`restore`(:141-153)、`relocate`(:156-164)、`addEdge/addGlossary/removeGlossary/awaken/setWorldTime`(:166-205)。
- **展示组件区(:211-370)**:`editorLabel`(:211-220,与 node.js 分叉)、`showDiff`(:223-265,LCS diff + `splitLines/diffOps` :247-285)、`confirmForget`(:287-312,三要素硬约束)、`confirmSimple`(:315-321,**被 databases.js import 的唯一 export**)、`openDialog`(:324-369,**modal 实现一:无 × 按钮**)。
- 工具(:372-452):`writeError`、`h`、`paramOf`、`toast`、`navigate`、`readCurrentVersion`、`nodeOf`、`versionOf`、`describeWriteError`。
- **mountEditor(:465-1360,905 行巨函,00-context 点名必须拆解)**:
  - state/事件绑定(:466-500)、`load`(:502-521)、`onClick`(:523-561,13 个 action 分发)、`onChange`(:564-577,importance 防抖 400ms)、`onSubmit`(:569-577)、`field`(:580-583)、`withBusy`(:585-593)。
  - 12 个 run* 动作(:596-948):runCreate(:596,覆盖保护)、runSaveContent(:639,乐观锁三选)、runSaveMeta(:703,entry-scope 写入)、submitImportance(:731)、runForget(:746,子树计数 + confirmForget)、runRestoreRevision(:774,先 diff 后确认)、showRevisionDiff(:808)、runRelocate(:821,ack 勾选强制)、runAddEdge(:857,悬空边确认)、runAddGlossary(:886,改挂确认)、runRemoveGlossary(:915)、runAwakenAdd(:925)、runSetWorldTime(:940,相对量基准确认)。
  - 渲染:`render`(:955)、`renderNotice`(:995)、`renderCreateForm`(:1010)、`renderContentForm`(:1030,入口 chip、stub/shadowed chip)、`renderMetaBlock`(:1066,range 重要度 + entryHint)、`renderRevisions`(:1121)、`renderDanger`(:1182)、`renderRelocate`(:1193,级联边警告)、`renderEdges`(:1224)、`renderGlossary`(:1265)、`renderAwaken`(:1297)、`renderWorldTime`(:1314)、`entryHint`(:1343-1358)。
- **恢复页 mountRestorePage(:1370-1572)**:分组/展开状态、`load`(:1395)、`runRestore`(:1432)、`render`(:1450)、`renderNotice`(:1470)、`renderBody`(:1485-1572,最新 URI 可恢复、历史地址折叠、两段陷阱警告)。
- 纯函数与收尾:`groupDeleted`(:1574-1606,导出供单测)、`depthOf`(:1608)、`countSubtreeSize`(:1621)、`labelOf`(:1635)、`confirmStale`(:1644-1699,**modal 实现二,同为裸 dialog,无 × 按钮**)。

### 9.2 迁移后结构(905 行巨函的拆解)

```
views/edit.js(目标 ~1150-1250 行;单文件保持,函数级拆解,不动路由表)
  import { get, post } from "../app.js"        ← 字面保留(V7b,D2 已确认)
  import ui(el/modal/showDiff?/dataTable/uriLine/badge 系/asyncPage/fmt 系)
  ── §A 写原语(:31-205):逐字保留,仅 notifyChanged 内部实现不变
  ── §B diff:showDiff/splitLines/diffOps → ui 收编(纯函数,零依赖),本文件 import
  ── §C 危险动作组件:
       confirmForget/confirmSimple/confirmStale 保留为「内容构建器」(各 ~15-25 行),
       dialog 骨架全部交 ui.modal(F1:×、遮罩、Esc、焦点归还)
  ── §D 编辑器:mountEditor 拆为
       mountEditor(装配:state + 委托 + load,~80 行)
         + bindEditorEvents(state, actions)      (onClick/onChange/onSubmit 分发表)
         + actions = { create, saveContent, saveMeta, forget, restoreRev, relocate,
                       addEdge, addGlossary, removeGlossary, awakenAdd, setWorldTime }
           —— 每个 run* 用 action(fn) 组合子统一 withBusy+try/catch+notice 管线
         + sections/{createForm,contentForm,metaBlock,revisions,danger,relocate,
                     edges,glossary,awaken,worldTime}.render(node, state)   (纯渲染函数组)
  ── §E 恢复页:mountRestorePage + groupDeleted/depthOf(逐字,单测锚点)
  ── §F 工具:writeError/describeWriteError/readCurrentVersion/nodeOf/versionOf/labelOf 保留
  删除:h/paramOf/toast/navigate 本地、openDialog、confirmStale 的 dialog 骨架、editorLabel(→ ui)
```

拆解原则:**行为零变化,只有结构**。所有 `data-action/data-form/data-field` 委托键、`state.notice` 文案、乐观锁三选、entry-scope 语义逐字保留;拆解的验收 = §13.8 的全动作走查。

### 9.3 用的公共件

`el`、`modal`(三处确认 + 冲突三选 + 恢复确认,F1 消费方)、`showDiff`、`dataTable`(修订史表)、`uriLine`(头部 URI :1035、入口 chip :1039-1046、归档地址列 :1154)、`impBadge`(metaBlock 重要度当前值旁)、`stubBadge/shadowedBadge`(renderContentForm :1047-1048)、`asyncPage`(loading/错误卡 :956-993、恢复页 :1450-1467)、`editorLabel`(统一版)。

### 9.4 DOM/交互变化

- **F1(消费)**:删除确认、通用确认、版本冲突三选、恢复确认全部换 `ui.modal`——右上角 ×、遮罩可关(删除确认**禁用**遮罩关闭,危险操作防误触)、Esc、焦点归还触发按钮。
- **F2**:编辑页头部裸 URI(:1035)与恢复页 `code` URI(:1514)换 `uriLine`;修订史「归档地址」列(:1154)等宽省略 + title + 复制。
- **F6**:占位/遮蔽/入口 chip 徽章化。
- **F7**:编辑页顶部新增 `uriBreadcrumb`(数据用 `dto.path`,与 node.js 同源;入口别名在链尾以 chip 标注,不造假层级)。
- **保持不变**:进页 `action=forget` 直弹高危确认、importance 防抖就地提交、relocate 的 ack 强制勾选、glossary 改挂/edge 悬空确认文案、世界钟相对量基准提示。

### 9.5 行数预估

1699 → **约 1150-1250**(-26%~-32%)。构成:openDialog+confirmStale 骨架 → modal -80;h/paramOf/toast/navigate -35;showDiff 迁出 -40;asyncPage 收拢 5 处三态 -90;run* 组合子统一 withBusy/try/notice -60;sections 拆解中 label/form 样板经 01 表单基类收紧 -120;dataTable/uriLine/徽章 -60;F7 面包屑 +10。

### 9.6 验收路径

`#/edit?mode=create` 新建(含覆盖确认);`#/edit?uri=…` 改正文(乐观锁:另开标签改同一节点后保存 → 三选弹窗,三按钮 + × + Esc 各验一遍)、改重要度(range 防抖)、改想起条件(别名入口进 → entryHint 正确)、删除(子树计数 + 三要素文案 + 遮罩不可关)、恢复修订(先 diff)、移动(relocate ack + 边警告)、加边(悬空确认)、加/删触发词(改挂确认)、醒来清单、世界钟(+1d 相对量);`#/deleted` 分组恢复(历史地址折叠、父先子后)。

---

## 10. 修复清单逐项落地(F2 / F4-F9)

> 每项给出:现状 → 方案 → 涉及文件 → 验收标准。F1/F3/F10 不在本节(归 02/02/04);edit.js 对 F1 的消费见 §9.4,侧栏对 F10 的预留位由 02 的壳设计决定(本文仅要求:侧栏「系统视图」分组之上保留一个图谱入口挂点,由 04/02 填)。

### 10.2 F2 · URI 完整可读与复制

**现状**:`id…` 式截断主要来自 UI 无约束折叠与 CSS 折行——树行 URI 靠 CSS 省略但无复制(app.css:53)、搜索卡 `word-break: break-all` 折行(app.css:116)、库管理路径逐字折行(app.css:252)、audit 对象列 CSS 省略无复制(app.css:226-230)。各处裸 `code`/`a` 无统一交互。

**方案**:`ui.uriLine(uri, opts)` 为全站唯一 URI 呈现组件:

- DOM:`<span class="mw-uriline"><button class="mw-uriline__text" type="button" title=uri>完整 URI 文本</button>(可选 <a class="mw-uriline__link">)</span>`;
- `.mw-uriline__text`:等宽(var(--mw-font-mono),01 定)、单行、`overflow:hidden; text-overflow:ellipsis`;`title` 恒为完整 URI;
- 点击 `__text` → `copyText(uri)` + `ctx.toast("已复制")`(mw:toast 契约,依赖 02 的 toast 监听修复,§15);
- `opts.href` 存在时并置一个真实 `<a>`(中键/新标签语义不丢);`opts.compact`(树行/下拉旁)省略 link 只留复制。
- **替换点**:tree 行(F8 内)、search 卡标题、raw entry_id/session、views 七页全部节点链接、audit 对象列、node 头部/子节点/边列表/技术信息、edit 头部/归档地址/恢复页、databases 路径列。

**验收**:① 全站 `grep` 九视图源码无 `…` 字面截断(content 摘要类截断如 `snippet()`/audit details 80 字**不算**,那是正文不是 URI);② 任一 URI 悬停出全文、点击后剪贴板 = 完整 URI 且 toast;③ 链接语义(中键新标签)在带 href 的位置仍工作。

### 10.3 F4 · raw 页去参数名 + session 人类标签(**主方案 B:服务端加 `first_text`,Main 已批准**)

**现状**:
- 参数名暴露:`from raw_id`/`to raw_id` 输入占位符(raw.js:299、:307);
- session UUID 暴露:下拉选项 `sessionOptionLabel`(raw.js:547-550,选项文本以 `session_id` 开头)、行头裸 `row.session_id`(raw.js:392);
- 服务端 `SessionDTO`(serialize.ts:166-174)= `{session_id, total, active, first_raw_id, last_raw_id, wall_first, wall_last}`,**没有首条消息文本**;`getSessions`(routes.ts:470-497)一条 GROUP BY 出全量。

**方案 B(主案,服务端最小改动)**:
1. `serialize.ts` `SessionDTO` 增加显式字段 `first_text: string | null`(白名单原则:显式字段,非透传);
2. `routes.ts getSessions`:GROUP BY 结果到手后,**按已有的 `first_raw_id` 做一次批量取文本**(`SELECT raw_id, substr(text,1,120) FROM raw_log WHERE raw_id IN (…)`),TS 侧折叠空白(`\s+→" "`)并截 80 字为 `first_text`;**不用 GROUP BY 相关子查询**(Main 指示:避免大 raw_log 库上的相关子查询成本;first_raw_id 已在索引路径上);
3. 前端选项标签(raw.js `sessionOptionLabel` 重写):
   `09-15 08:30 · 42 条 · 活跃 40 ·「商队从北方来,商队要经过…」`
   —— `humanTime(wall_first)` + total/active + 首条摘要;UUID 从选项文本消失;
4. 行头去 `row.session_id`(raw.js:392 删);UUID 只在两处等宽出现:行「详情」dl(raw.js:419-420,换 `uriLine` 获得复制)与选中会话后的头部说明行(raw.js:340,现有 `sessionMeta` 回显,同样换 `uriLine`);
5. 深链 URL 的 `session=` 参数**保留**(功能性参数,不是展示标签;`rawDeepLink` raw.js:561-564 不动)。

**方案 A(降级,纯前端)**:服务端未升级(旧实例 `first_text === undefined`)时,标签退化为 `09-15 08:30 · 42 条 · 活跃 40`(wall_first 纯前端可得)。与多库 404 旧实例的优雅退化同一模式(databases.js:265-280 先例)。

**定位输入**:替掉 from/to 两个数字框(raw.js:296-313):
- 单输入 `placeholder="定位:输入编号,如 123 或 #123"`,`parseLocateInput(v)`(纯函数,导出供单测):`/^#?(\d+)$/` → `{ from: N }`;不匹配 → 行内错误「请输入数字编号(原文行的永久编号)」,不发请求;
- 提交 → `q.from = N`(窗口自 N 开价,`limit=50`);「高级」折叠里保留双框范围模式(占位「起始编号 / 结束编号」,仍不带 raw_id 字样),兼容原 from+to 语义(onSubmit raw.js:238-249 结构保留);
- 头部「数据源是 raw_log…轴序按 raw_id」说明行(raw.js:316-320)保留——它解释的是编号的含义,不是参数名。

**涉及文件**:`src/web/serialize.ts`(+1 字段)、`src/web/routes.ts`(getSessions +8 行)、`src/web/assets/views/raw.js`、`app.css`(定位表单)、`ui.js`(humanTime)。

**验收**:① 页面可见文本 `grep raw.js` 无 `raw_id` 字样出现在控件占位/标签中(说明行正文除外);② 输入 `#30`/`30` 都定位到 raw_id=30 起始页,`abc` 行内报错;③ session 下拉无 UUID、含时间/计数/摘要;④ 旧版服务端(A 档)标签不含摘要但不破版;⑤ `curl /api/sessions` 每项含 `first_text`(字符串,≤80 字,无换行)且其余字段不变;⑥ api.test.ts:547-638 既有断言全绿(total/totalAll/唯一性不受影响)。

### 10.4 F5 · 库管理行布局

**现状**:app.css:252 `.mw-db-path { word-break: break-all }` 使长路径逐字符竖排折行;行内「(进程库)」是裸文本 div(databases.js:90)。

**方案**:
- `pathCell`(databases.js:50-52)→ `uriLine(path)`(等宽、单行省略、title、点击复制);
- 行布局改 CSS Grid:`grid-template-columns: minmax(9rem, 14rem) 1fr 5rem 10rem 8rem auto`(label+进程徽章 / 路径弹性 / 节点数 / 世界时间 / 状态徽章 / 切换钮),窄屏(<720px)降两级堆叠;
- `(进程库)` 与「可达/不可达:err」→ `badge`(进程库 = 强调徽章;不可达 = 红描边徽章,`title` 载完整错误文本,databases.js:106-108 的降级文案保留在 title);
- app.css 删 `word-break: break-all` 那条(01 统一删,本文登记依赖)。

**验收**:300px-1280px 宽度扫描下行高恒定、路径永不逐字折行、悬停见全路径、点击复制;切换/打开/新建/诊断行为与现状一致(§8.6)。

### 10.5 F6 · 元数据徽章

**现状**:三份 importance 分档实现不一致——tree.js:45-53(五档含 0 单独档)、temp.js:60-72(分叉副本,含死分支 `v>=6`)、views.js:85-88(裸 `★N` 无档);source 是裸 chip(node.js:37-40、temp.js:344);stub 靠 `◌`/「占位」裸文本散落(tree.js:253、:255、temp.js:341、views.js:454)。

**方案**(`ui.js`,01 出皮):
- `impBadge(n)`:四档——`9-10 → mw-imp--4`、`7-8 → mw-imp--3`、`4-6 → mw-imp--2`、`0-3 → mw-imp--1`;`title="重要度 N(10 = 最重要)"`;**0 不再单独一档**(契约 F6 四档口径;旧 `--5` 类废止,app.css:91-101 的五档规则由 01 重写);
- `sourceBadge(source)`:manual/auto/import 三色 + 未知值灰底原样(容错纪律同 node.js:39);
- `stubBadge()`:灰纹底座 + 文案「占位」,替换 `◌`/「占位」裸文本;
- `shadowedBadge/roleBadge/eventBadge/discBadge/discChip`:迁入 ui,语义与文案逐字冻结(§0.3)。
- 替换点:tree 行 tail、node 头部/子节点、search 卡、raw 行头、temp 行、views 七页、edit 内容表单。

**验收**:① 四档视觉两两可辨(01 色板);② 任意视图同一 importance 数值渲染同一档;③ source=未知字符串不抛错原样显示;④ 全站不再出现裸 `★N` 文本(grep `★${` 只在 ui.js 命中)。

### 10.6 F7 · 面包屑

**现状**:node 页 `renderBreadcrumb`(node.js:69-81)把每个**祖先的完整 URI** 当链接文本——三层以上就撑爆;tree 页已有段名面包屑(tree.js:651-664)但与 node 各写一套;edit 页无面包屑。

**方案**:`ui.uriBreadcrumb({uri, domain?, path?, hrefFor?})`:
- 有 `path`(node/edit 的 `dto.path`,服务端按 implicitParentUri 算)用之;无则按 `parentOf` 逐级切段(tree 场景,沿用 tree.js:651 现行为);
- 渲染:`domain://`(根,链 `#/tree?domain=…`)/ 每级 `lastSegment`(链 `#/tree?uri=<祖先完整 uri>`)/ 当前节点(aria-current,`title` 全 URI,点击复制);
- node.js:69-81、tree.js:651-664 删除,edit.js renderContentForm 顶部新增(node/edit 用 `dto.path`,遵守 node.js:67-68「不自己切 /」纪律——仅在树场景沿用既有切段行为)。

**验收**:① `core://a/b/c` 页顶出现 `core:// / a / b / c` 逐级可点,各级落点分别是根层/a 层/b 层的树视图;② 当前级不可点且 aria-current;③ edit 页同构;④ 别名入口打开 node 页时(path 来自规范 uri 的DTO),当前显示为入口 uri、不伪造规范层级。

### 10.7 F8 · 树行重构

**现状**:tree.js:193-298 行 = caret + `● ` 裸符号 + `lastSegment`(只有末段,前缀不可见,title 里有全文)+ snippet + tail(imp/disc/shadowed/↗);child_count 已随行模型返回(`flattened`,tree.js:163)但 UI 不显示、也不进 aria-label。

**方案**(`buildRow` 重排,其余树机制不动):
```
[caret(button,aria-expanded)] [mw-uri(role=treeitem,href)]
    ├─ mw-uri__prefix   core://identity/   (等宽、muted、小号、flex:0 1 auto 先被压缩)
    ├─ mw-uri__seg      habits             (主视觉,flex:0 0 auto 不压缩)
    ├─ mw-uri__count    ×12                (has_children 且 child_count>0 时,muted 小号)
  [mw-snippet(content_head,现状保留)]
  [tail: impBadge | discChip | stubBadge | shadowedBadge | ↗]
```
- 前缀 + 段名拼起来 = 完整段名(F8「显示完整段名」);压缩次序:snippet 先消失 → 前缀省略 → 段名永不截断(CSS `min-width` 保证,01 落皮);
- aria-label 保持现状公式(tree.js:234-252:段名、重要度、想起条件、遮蔽),新增 child_count 一段;
- `peek`/键盘/窗口化/缩进(depth×0.85rem,tree.js:203)全部不动。

**验收**:① 任意行完整 URI 可读(前缀弱化但存在,悬停 title 全文);② 叶子行无 caret 占位、分支行有 ×N;③ 28px 定高不破(窗口化 `--mw-row-h` 同源,tree.js:295-299);④ 键盘漫游、peek、↗ 与现状一致。

### 10.8 F9 · 搜索高亮(**核查结论:已实现;增强为命中窗口,纯前端,Main 已批准**)

**现状核查**:
1. **高亮已存在**:`splitQuery`(search.js:21-27,CJK 段 + 拉丁词,不做 bigram)+ `highlight`(search.js:32-77,indexOf 重叠扫描 → 区间合并 → 一次性 DocumentFragment,`<mark>` + textContent,保留原大小写),已作用于结果卡摘要(search.js:89-91);并有触发词命中的诚实边界提示(search.js:120-124)。**F9 的"先核实现状是否已有"答案:有。**
2. **服务端字段是否够**:`SearchItemDTO`(serialize.ts:209-222)含 `summary` **和全文 `content`**(routes.ts:366 回传,`summarize()` 在 recall.ts:82-85 只做头部截断)。**够,不需要服务端任何改动。**
3. **唯一缺口**:命中点落在 `summary` 截断窗口之外时(长正文尾部命中),卡片上既无高亮、又因诚实性检查把 `summary+content` 拼一起查字面量(search.js:121)而不触发「触发词命中」提示 → **静默无痕**。

**方案(纯前端)**:`ui.hitExcerpt(content, terms, radius=80)`——在 `content` 上找首个任一词的命中下标,取 `±radius` 窗口,过 `highlight()` 出带省略号的摘录;`renderCard` 逻辑改为:
```
const sumHit  = splitQuery(q).some(t => summary.toLowerCase().includes(t.toLowerCase()));
const bodyHit = splitQuery(q).some(t => content.toLowerCase().includes(t.toLowerCase()));
if (!sumHit && bodyHit) 显示 hitExcerpt(替换摘要位,注明「命中在正文中」)
else                     显示 highlight(summary)(现状)
诚实提示(search.js:120-124)保持:仅当 summary 与 content 都无字面命中时才出「触发词命中」
```
- 迁移时 `splitQuery/highlight` 移入 `ui.js`(纯函数,可被 `.js` 测试真实 import,同 disc-badge 模式);search.js 只消费。

**验收**:① 中文词(如「薇拉」)命中处 `<mark>` 高亮;② 构造长正文尾部命中的节点:卡片出现「……命中窗口……」摘录且窗口内高亮;③ 触发词命中(正文+摘要都无该词)提示照旧;④ `mark` 样式与暗色三态可辨(01 出皮);⑤ 大小写:查询 `VERA` 命中 `Vera` 原样显示。

---

## 11. 行数预算:总降幅 ≥25% 的可达性论证

现状合计 **5447**(实测 `wc -l`)。目标 **≤ 4085**(-25%)。

| 文件 | 现状 | 目标 | 降幅 | 减量机制(可核查的杠杆) |
|---|---|---|---|---|
| tree.js | 784 | ~660-690 | -12%~-16% | 纯助手迁出(~50)+ 面包屑/select 组件化(~23);窗口化/键盘/缓存三块(~330 行)承重不减 |
| node.js | 480 | ~380-410 | -15%~-21% | editorLabel/kv/section/dash 迁出(~40)+ revisions dataTable(~12)+ 徽章化(~10) |
| search.js | 279 | ~160-180 | -36%~-43% | splitQuery/highlight 迁出(57)+ select/asyncPage(~35)- F9 增强(+12) |
| raw.js | 590 | ~480-510 | -14%~-19% | h/paramOf/wrapper/copyText(~60)+ select/pager/asyncPage(~35)+ F4 重做(±15) |
| temp.js | 420 | ~330-355 | -16%~-21% | h/snippet/impClass/num(~45)+ asyncPage/select(~30)+ 徽章化(~10) |
| views.js | 657 | ~460-500 | -24%~-30% | 徽章真相源迁出(~85)+ 七渲染脚手架收紧(~60)+ dataTable/select/asyncPage(~50) |
| audit.js | 225 | ~150-165 | -27%~-33% | h/fmt/eventBadge(~35)+ dataTable/pager(~30) |
| databases.js | 313 | ~250-270 | -14%~-20% | table()/num/txt(~26)+ 表单皮(~15)+ confirm 换源(~6) |
| edit.js | 1699 | ~1150-1250 | -26%~-32% | dialog 骨架归一(~80)+ 三态收拢(~90)+ run* 组合子(~60)+ 样板收紧(~180)+ showDiff/h/wrapper(~75) |
| **合计** | **5447** | **~4030-4230** | **-22%~-26%** | |

**论证**:中位情形 ~4130(-24.1%),悲观 ~4230(-22.3%),乐观 ~4030(-26.0%)。要稳过 25% 线,前提是四个大杠杆按期落地:① `h()`→`el` 统一(五份,~115 行);② modal 归一(~100 行,edit.js 为主);③ dataTable/pager/select/表单基类脚手架(~250 行);④ asyncPage 三态收拢(~120 行)。这四项全部是**纯机械重复**的消灭,不存在语义风险。若 02 的脚手架延期,兜底杠杆依次为:edit.js run* 组合子进一步收拢 describeWriteError 管线(~40)、views.js 七渲染的 listRenderer 进一步抽型(~30)、node.js 边/别名/触发词三区块合并 section 工厂(~20)。**质量优先**:若实现中发现某处压缩会模糊语义(如 windowing、乐观锁),保语义、放弃行数,并在实现 PR 里记录放弃点——00-context 允许"质量优先于行数本身"。

---

## 12. 受影响测试清单(逐文件,三类:意图保留 / 字面量更新 / 删除;另设"不受影响")

> 前置事实(已逐一读源码核实):**views-parity.test.ts 与 snippet-guard.test.ts 都不测前端**——前者钉 `src/web/views.ts`(DTO)与 `src/memory-views.ts`(文本)双侧 parity(test/web/views-parity.test.ts:1-15,唯一涉 assets 处是起服务器的 assetsDir,:718);后者钉 `src/memory-views.ts` 的 `snippet()` 折叠(test/web/snippet-guard.test.ts:11-19)。**这次前端重设计对两者零影响。**

### 12.1 test/web/multi-db-frontend.test.ts(静态哨兵,读源码断言)

| 断言 | 现钉内容 | 分类 | 处理 |
|---|---|---|---|
| V7a(:66-71) | edit.js 中 `fetch(` 计数 = 0 | **意图保留,零改动** | 迁移后 edit.js 仍零 fetch(§0.2 第 3 条) |
| V7b(:73-77) | edit.js `import {…get…} from "../app.js"` 正则 | **意图保留,零改动** | D2 已确认该 import 字面保留 |
| V7c(:79-95) | app.js 双注入点 + 禁手拼 db= | 不受 03 影响(02 域) | — |
| V11a(:100-111)/V11b(:113-122) | tree-cache.js 的 ensureCacheFor/clear/gen++ | **意图保留,零改动** | tree-cache.js 不动 |
| V11c(:124-136) | 树缓存 db 归属;tree.js 不得自持 `export const treeCache`、必须含 `from "./tree-cache.js"`;文件清单含 tree/databases/edit 的 `new Map()` 扫描 | **意图保留,零改动** | tree.js 迁移保留该 import; databases/edit 是否留 Map 自由(断言只要求清单含 tree-cache.js) |
| V11d(:139-152) | loadLayer:`const gen = currentGen()` 在 await 前、`if (gen !== currentGen())` 在 await 后(按源码 index 顺序) | **意图保留,零改动** | loadLayer 逐字搬运(§0.2 第 10 条);**迁移 PR 若改动这两个文本形状(哪怕加注释插入其间不违法——只查先后),仍建议保持逐字** |
| V11 control(:154-161) | `parentOf` 只在 tree-cache.js 导出 | **意图保留,零改动** | tree.js 不得 re-export parentOf |
| V12a(:164-223) | app.js reloadAll 清缓存先于 remount、switchDatabase 调 reloadAll | 02 域 | — |

### 12.2 test/web/multi-db-tree-cache.test.js(行为测试,直接 import 纯模块)

**不受影响,零改动**(tree-cache.js 一行不改;其 import 路径与导出形状是冻结不变量 00-context §2.5)。

### 12.3 test/web/multi-db-url.test.js

**不受影响,零改动**(url.js 不动;V13 系列纯函数断言全部照旧)。

### 12.4 test/web/disc-badge.test.js

- 现状:`import { discBadge, discChip, foldWs } from "…/assets/views/views.js"`(:15),假 DOM(installDom :24-53)真实驱动。
- **分类:字面量更新**——discBadge/discChip/foldWs 迁入 ui.js 后,import 路径改 `…/assets/views/ui.js`(或 02 终定路径);**全部断言逐字保留**(空值返回 null、entry/edge 变体、title 组装、foldWs 折叠、append 风格无字面量 "null")。
- 可行性已由 02 的 ui.js 约束保证:零 import、零顶层副作用、`ui.el` 只用最小 Node API(D2 承诺,§0.1)——假 DOM 能继续驱动。
- 文件名不改(`disc-badge.test.js` 仍名副其实);若 02 决定不迁移徽章,则本文件零改动(二选一,以 02 文档为准,已对齐)。

### 12.5 test/web/views-parity.test.ts

**不受影响,零改动**(服务端双侧 parity;前端 views.js 重命名/重构不触及。注意文件名撞车:测的是 `src/web/views.ts`,不是 `assets/views/views.js`)。

### 12.6 test/web/snippet-guard.test.ts

**不受影响,零改动**(钉服务端 `memory-views.ts` 的 snippet 折叠;前端 temp.js/views.js 里的同名工具迁移不影响它)。

### 12.7 test/web/dto-contract.test.ts

- 现有断言:/api/view 判别联合、EdgeDTO 不变量、disclosure 三表字段(:91、:182、:224…)——**前端迁移零影响**。
- **F4-B 新增(建议,Main 已点名)**:补一条「SessionDTO 含 `first_text: string|null`,且 ≤80 字、无换行」的显式断言(白名单字段显式存在的回归位);同时可加「NodeDTO 不含 first_text」的反向断言强化白名单纪律。**只增不删。**

### 12.8 test/web/api.test.ts

- :547-638 `GET /api/raw and /api/sessions`:total/totalAll 过滤口径、session 唯一性、active≤total——**F4-B 全部保持绿**(加字段是 additive);建议同处补 `first_text` 类型断言。
- :477-478 `Object.hasOwn(auto, "first_raw_id") === false` 钉的是 **NodeDTO** 不漏 raw 字段——与 SessionDTO 加字段无冲突(白名单按 DTO 各自枚举)。

### 12.9 其余

- audit.test.ts:服务端 /api/audit(分页/事件全集/只读/不聚合,:179-:279)——**不受影响**。
- multi-db-api.test.ts:411-414 的路由冒烟含 `/api/sessions`——additive 字段不破坏。
- security/visibility/multi-db-{registry,probe,lock,cli}/dto 之外的测试:不涉及前端。

### 12.10 建议新增(可选,非验收门槛)

- `disc-badge.test.js` 模式扩展:对 `ui.highlight/splitQuery/hitExcerpt` 的 `.js` 真实 import 行为测试(F9 的纯函数锚点)。
- raw.js `parseLocateInput` 的纯函数单测(`#123`/`123`/`abc`/空串)。

---

## 13. 迁移顺序与手工验收路径

> 原则:先小且隔离(练熟 ui 原语)→ 中等只读页 → 语义冻结多的 DTO 页 → 写路径巨页 → 承重墙(树)。每步独立可发布、可回滚;**前置依赖只有 02 的 ui.js 对应子集落地**(各步标注所需子集)。启动样例库:`~/projects/pi-memory-web --db ~/projects/amio/elias/.pi/memory.db --port 8791`(125 节点/5 域/100 raw/49 glossary,00-context §6)。

| 步 | 视图 | 风险 | 所需 ui 子集 | 手工验收(浏览器点哪里、看什么) |
|---|---|---|---|---|
| 1 | **audit.js** | 最低:225 行、单端点只读、无路由纠缠 | el/dataTable/pager/eventBadge/uriLine/asyncPage/fmt | `#/audit`:翻页两端禁用态;九组事件徽章;对象 URI 点击复制(※ toast 需 02 的 toast 修复,未合前先看剪贴板);细节列 title 全文 |
| 2 | **databases.js** | 低:独立页、三端点已隔离 | +uriLine/badge/confirm | `#/databases`:F5 行布局(§8.6 全项);新建确认框验 F1(×/遮罩/Esc/焦点归还) |
| 3 | **search.js** | 低:纯读,行为有明确现状对照 | +splitQuery/highlight/hitExcerpt | `#/search`:§3.6 全项;与旧版并开对照命中一致 |
| 4 | **temp.js** | 低:只读 + 两个跳转按钮 | +impBadge/sourceBadge/shadowedBadge | `#/temp`:§5.6 全项 |
| 5 | **views.js** | 中:七视图语义冻结面大 | +dataTable/foldWs 迁入后的回归 | `#/view` 七 tab:§6.6 全项;重点 wakeup 去重与 diagnostic 清单 |
| 6 | **raw.js** | 中:F4 主案含服务端小改(先合 `first_text`,前端 feature-detect) | +humanTime/pager | `#/raw`:§4.6 全项;与 `#/view?name=timeline` 对照;旧版服务器(A 档)烟雾 |
| 7 | **node.js** | 中高:抽屉 + 十个渲染区块 + renderMeta 修 bug | +uriBreadcrumb/editorLabel 统一版 | §2.6 全项;树行 peek 进抽屉的回归 |
| 8 | **edit.js** | 高:写路径 + modal 收敛 + 巨函拆解 | +modal/showDiff | §9.6 全项(全动作走查);`#/deleted` 分组恢复;乐观锁双标签对抗 |
| 9 | **tree.js** | 最高:窗口化/键盘/缓存/侧栏/peek 五线 | 全量 | §1.6 全项;哨兵专项:切库后树缓存确实清空(两库同 uri 不同内容对照);>500 行窗口化滚动;`memory:changed` 后精确失效 |

每步完成后跑:`node "$(git rev-parse --show-toplevel)/node_modules/vitest/dist/cli.js" --run test/web/multi-db-frontend.test.ts test/web/disc-badge.test.js test/web/multi-db-tree-cache.test.js test/web/multi-db-url.test.js`(+ 第 6 步后加 api.test.ts 与 dto-contract.test.ts)。全量验证由 Main 收尾统一跑。

---

## 14. 对 01 / 02 的反向输入(依赖与请托)

1. **02 · toast 修复是 F2 验收前置**:app.js:251-253 在 `document` 上派发 `mw:toast`,监听却挂在 `#mw-toasts` 后代(app.js:765)→ 当前全站 toast 静默。02 修(监听上移 document);视图侧 `ctx.toast` 调用零改动。
2. **02 · ui.js 判据**:`node -e "import('./views/ui.js')"` 不抛;`ui.el` 最小 Node API 集(§0.1)——disc-badge.test.js 的假 DOM 依赖它。
3. **02 · edit.js 的 `import { get, post } from "../app.js"` 字面保留**(V7b);ui.js 零 HTTP。
4. **01 · 必须保留的 JS-读-CSS 契约**:`--mw-row-h`(tree.js:295-299 用 getComputedStyle 读,窗口化定高);`mark`(F9)、`mw-disc--{entry,edge,dead}`、`mw-raw--inactive`(斜纹,app.css:208-218)、`mk-*` 九组审计配色、`mw-` 前缀。
5. **01 · 暗色三态的双路径教训**:auto 态移除 `data-theme` 走 `prefers-color-scheme`、手动态设属性,**两路径都要覆盖**(app.css:57-59 的既有注释是血泪);新徽章色板同理。
6. **01 · 审计表固定布局**:列宽是按真 Chromium 逐格量的(app.css:225-250 注释),重皮时保留 `table-layout: fixed` + 单行省略 + `title` 承载全文的三层结构。
7. **02 · 侧栏 F10 挂点**:tree.js `mountSidebar` 的组装次序(tree.js:752-771)中,「系统视图」分组(viewHost)之上预留图谱入口槽(04 填充);本文不定义其形态。
8. **02 · index.html**:顶栏 `#mw-q`(搜索框)现状即 F3 的输入挂点;`#mw-drawer`(index.html:28)是 peek 抽屉的宿主,迁移中不改其 id/结构。

---

## 15. 风险与开放问题

1. **toast 死 bug**(§14.1):F2 的「复制 + toast」验收在 02 修复合入前只能验剪贴板。已在迁移顺序 §13 标注。
2. **`<option>` 原生截断**:F4 的人类标签仍受原生 select 的选项宽度裁剪(浏览器从中间截,项目无法控制——app.css:249-250 注释先例)。缓解:标签控制在 ~40 字符内(时间 11 + 计数 ~10 + 摘要 ≤24);完整信息在选中后的头部说明行。
3. **F8 与窗口化的耦合**:行内新增 `mw-uri__count` 不得改变 28px 定高(行高塌陷会让 `applyWindow` 的 `scrollTop/rowH` 数学失真);实现时 `rowHeight()`(tree.js:295)继续以 `--mw-row-h` 为唯一真相。
4. **edit.js 拆解的回归面**:905 行巨函拆解是纯结构变更,但改动行数最多;缓解 = §0.3 冻结清单 + §13.8 全动作走查 + 哨兵 V7a/V7b 持续盯着 HTTP 出口。
5. **temp.js enrich 的 N+1**(temp.js:148-175,最多 60 个 `/api/node`):既有妥协,本次**不修**(服务端批量端点不在契约内)。登记为后续债务;TEMP 页设计上就小(阈值 10、目标 0),病态库有 CAP 兜底。
6. **F4-B 的服务端改动**:虽经 Main 批准,仍属对「服务端骨架不动」边界的显式例外(00-context §1 只给 04 开了 /api/graph 先例)。落地时 serialize.ts(接口 + 字段)+ routes.ts(批量取文本)两处、合计 ~15 行,不触碰 store 层;若 Main 后续反悔,前端 A 档(§10.3)即降级路径,视图代码无需回滚——feature-detect 写法保证两端兼容。
7. **行数 25% 线的余量**:§11 论证中位 -24.1%,靠悲观杠杆(§11 兜底序列)补足;实现期每完成一个视图在本文表格回填实际行数,偏差 >10% 时向 Main 广播。
